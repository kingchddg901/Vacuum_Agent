/**
 * Default bundle: empty on purpose.
 *
 * With no overrides, every `--evcc-*` token resolves to its baked
 * `:host` default (src/styles/foundation.js + learning.js) behind the
 * HA-variable fallbacks — the deterministic default palette. Real
 * CVD-safe / theme bundles (Wave 4) and uploaded exports (Wave 5)
 * override on top of this.
 */
export default {};
