[
 {
  "finding_id": "#18:A3-IMAGE--1",
  "adjudicated_at": "2026-08-03",
  "verdict": "overstated",
  "reason": "The mechanism as written does not hold, and the proposed cure is worse than the disease. (1) Ids are NOT positionally reassigned. adapters/eufy/segmentor.py mints segment_id when a blob is ADMITTED (:1289 kept regions, :1474 count-deficit recoveries) and the id rides with that blob; the sorts at :1414 and :1485 reorder the LIST and renumber nothing. (2) The pipeline is deterministic -- no RNG and no set-iteration anywhere in the path -- so the same image with the same tuning yields byte-identical ids. A plain re-analysis therefore cannot reassign anything, which is what the finding's 'any re-analysis in which blob ordering shifts' claims. Ids can only shift when the admitted blob SET changes: tuning altered (min_area_pixels / expected_room_count / max_segments, via the dedup at :1416-1432, the recovery branch at :1444-1483, or the truncate at :1487), or a genuinely different map image. NEITHER TRIGGER IS REACHABLE FROM THE UI. The card has exactly two analyze call sites (src/bindings/map.js:1104 after an upload, :1204 for an explicit re-analyze) and both pass ONLY {force_reanalyze: true} -- no tuning parameters at all -- so a card-driven re-analysis always uses the adapter's declared tuning and is therefore deterministic and id-stable. Altering tuning requires a hand-written service call from YAML or dev-tools, i.e. a deliberate expert action on a computer-vision pipeline; Chris's position, and it is the right one, is that a user who hand-tunes a CV segmenter has picked up a foot-gun of their own making and does not need the backend to defend them from it. The other trigger, a different image, arrives via upload -- where the user has just supplied a new map, so the rooms genuinely changed and relinking is the expected outcome rather than a defect. (3) 'Silently' is wrong. The link injects room_id onto the segment and the card renders that room's name on the polygon, so a stale link paints the wrong room name onto a map of the user's own home -- among the most visible failure modes this integration has, and Chris confirms this subsystem surfaces its failures in practice. The residual real case is narrow: a re-tune that swaps two similar adjacent rooms where the wrong label is not obvious. (4) A fix was written and REJECTED: retiring links whose segment geometry moved. Re-tuning IS the intended workflow -- the CV output is human-corrected at the end via card-side translations, edge nudges and vertex moves -- so the guard fires precisely when the user is iterating on tuning and deletes hand-made room links at exactly the wrong moment. The rooms did not move; only the segmentation did. Trading a visible mislabel for silent destruction of the user's own work is a bad trade. Its 'old centre inside new bbox' test was also a hidden threshold, scale-dependent (a large room's bbox swallows any centre; a closet's retires on noise) and asymmetric -- and in a CV subsystem an explicit tuned threshold, e.g. bbox IoU declared in mapping.segmenter_tuning beside the existing knobs, is the correct idiom rather than something to avoid. If this is ever revisited, the shape is a per-link IoU CONFIDENCE surfaced to the card, never a backend deletion."
 },
 {
  "finding_id": "#13:A4-SETUP-6",
  "adjudicated_at": "2026-08-03",
  "verdict": "severity corrected HIGH -> LOW (latent)",
  "severity": "LOW",
  "reason": "MECHANISM CONFIRMED, HARM MISDESCRIBED. The scoping claim is correct and worth keeping: rejection is NOT map-scoped. `rejected_rooms` is a flat list[int] on setup_progress[vacuum_entity_id] with no map dimension, _get_progress_record is keyed by vacuum only, the suppression is a flat set subtraction applied identically to every map, and reject_rooms iterates manager.data['maps'][veid] with no filter -- while discovery IS map-capable (discover_rooms_for_vacuum takes map_id), each drift row carries a map_id, and the card renders it beside the button. The UI presents a per-map decision that the backend applies per-vacuum. But the finding's HARM -- 'silently loses that room's configuration on maps they were not looking at' -- is unreachable. new_candidate_ids = discovered - configured_ids - rejected, and _list_configured_room_ids collects across ALL maps for the vacuum, so a room configured anywhere is never offered for rejection anywhere. The rooms.pop therefore removes a discovery-created stub, not user data. And the user guide (docs/user-guide/11-setup.md, applying charter delta 9) shows the other two 'harms' are the FEATURE: phantom rooms 'need to be rejected here so they don't become managed entities', and Reject as phantom 'permanently suppresses the room... never appears in this list again... Use this for ghost rooms the firmware occasionally invents.' Permanence is the point, and a typed-token gate on dismissing a firmware ghost would be absurd friction. Chris: it refuses the room's CREATION, it does not delete a room. The card offers it only inside Setup; anything else needs a hand-written service call. WHAT SURVIVES, and why it stays OPEN at LOW rather than being retired: rejection is vacuum-scoped while phantoms are per-map, and Eufy reuses ids 1-11 per map. A genuine phantom id on one floor therefore permanently blocks configuring a REAL room with the same id on another, and there is no un-reject path (grep: the only writers are drift.py and core/manager.py:559 record-init). That is the inverse of the finding's harm -- not losing existing configuration, but losing the ABILITY to configure. Latent today because only one map per vacuum carries rooms in this fleet; that is current state, NOT a limit (Chris), so it becomes live the day a second floor is configured. Minor second residual: reject_rooms never checks is_configured, so a direct service call can pop a configured room. Not reachable from the card. FIX SHAPE when it is worth doing: carry the map the rejection was made on rather than adding a confirmation gate or a protection level -- neither of those addresses the actual defect. Reads stay tolerant of the legacy flat list (treat as all-maps, preserving today's behaviour for existing records); writes become map-keyed; the service takes an optional map_id and the card passes the map_id it already renders."
 },
 {
  "finding_id": "#18:A2-POLYGO-5",
  "adjudicated_at": "2026-08-04",
  "verdict": "overstated",
  "reason": "OVERSTATED — the premise does not hold for any reachable trigger, and the proposed cure would have caused the harm it was guarding against. Both findings assume a re-analysis can reassign segment_N to a DIFFERENT polygon, so a stored nudge would move a room the user never edited. EMPIRICAL DISPROOF (Chris, 2026-08-04): he cleared the adjustments and forced re-analysis THIRTY times — everything stayed stable. Ids moved only when he loaded NEW images. That is the same determinism already established in the #18:A3-IMAGE--1 adjudication (no RNG, no set-iteration in the path; the id rides with the admitted blob), now confirmed by direct observation rather than by reading the code. And a new image is not this defect: the user has just replaced the map, the rooms genuinely changed, and re-nudging is the expected outcome — the same reasoning that retired A3-IMAGE--1. WHY THE FIX WAS WORSE THAN THE BUG: the specced token was image_segments['analyzed_at'], a wall-clock stamp minted fresh on EVERY successful analysis (mapping_services.py:1165) with no content fingerprint anywhere in the module. Since a re-analysis yields identical ids and a NEW timestamp, the gate would have suppressed every valid adjustment on every re-analysis — and the card force-re-analyses on each CV-variant upload (src/bindings/map.js:1104), so a user who dragged an edge and then uploaded a variant would silently lose the nudge. Precisely the 'destroys hand-made work at exactly the wrong moment' failure A3-IMAGE--1's fix was rejected for. A stamp keyed on a CONTENT fingerprint rather than a timestamp would be sound, but with the trigger disproven there is nothing left for it to protect. SCOPING CLAIM ALSO WRONG. The spec carried a 'secondary leak' where leftover CV adjustments apply to CUSTOM segments. The two systems are separate by construction — CV mints segment_N (adapters/eufy/segmentor.py:1289), custom mints custom_N (mapping_services.py:1685), into separate stores resolved by _resolve_active_scope. They cannot collide unless a hand-written service call supplies a colliding id, which is a deliberate affordance (caller-supplied ids let segment_room_links survive an edit), not a defect. Chris: the custom polygon system is completely separate from CV. The spec's 'optional, recommended' half — making adjust_map_segment scope-resolved so it works in custom mode — is therefore REJECTED outright: adjust is a CV nudge tool, and the custom system has its own editing path (set_custom_segments replaces polygons wholesale). ONE RESIDUAL, tracked separately and NOT part of these findings: _handle_get_map_segments applies image_segment_adjustments to scope['segments_store'] with no mode check (mapping_services.py:1210), so in custom mode it consults a CV-only store. Inert today because the namespaces differ; it is a latent aliasing seam and a one-line guard, not the defect filed here."
 },
 {
  "finding_id": "#18:A4-CUSTOM-6",
  "adjudicated_at": "2026-08-04",
  "verdict": "overstated",
  "reason": "OVERSTATED — the premise does not hold for any reachable trigger, and the proposed cure would have caused the harm it was guarding against. Both findings assume a re-analysis can reassign segment_N to a DIFFERENT polygon, so a stored nudge would move a room the user never edited. EMPIRICAL DISPROOF (Chris, 2026-08-04): he cleared the adjustments and forced re-analysis THIRTY times — everything stayed stable. Ids moved only when he loaded NEW images. That is the same determinism already established in the #18:A3-IMAGE--1 adjudication (no RNG, no set-iteration in the path; the id rides with the admitted blob), now confirmed by direct observation rather than by reading the code. And a new image is not this defect: the user has just replaced the map, the rooms genuinely changed, and re-nudging is the expected outcome — the same reasoning that retired A3-IMAGE--1. WHY THE FIX WAS WORSE THAN THE BUG: the specced token was image_segments['analyzed_at'], a wall-clock stamp minted fresh on EVERY successful analysis (mapping_services.py:1165) with no content fingerprint anywhere in the module. Since a re-analysis yields identical ids and a NEW timestamp, the gate would have suppressed every valid adjustment on every re-analysis — and the card force-re-analyses on each CV-variant upload (src/bindings/map.js:1104), so a user who dragged an edge and then uploaded a variant would silently lose the nudge. Precisely the 'destroys hand-made work at exactly the wrong moment' failure A3-IMAGE--1's fix was rejected for. A stamp keyed on a CONTENT fingerprint rather than a timestamp would be sound, but with the trigger disproven there is nothing left for it to protect. SCOPING CLAIM ALSO WRONG. The spec carried a 'secondary leak' where leftover CV adjustments apply to CUSTOM segments. The two systems are separate by construction — CV mints segment_N (adapters/eufy/segmentor.py:1289), custom mints custom_N (mapping_services.py:1685), into separate stores resolved by _resolve_active_scope. They cannot collide unless a hand-written service call supplies a colliding id, which is a deliberate affordance (caller-supplied ids let segment_room_links survive an edit), not a defect. Chris: the custom polygon system is completely separate from CV. The spec's 'optional, recommended' half — making adjust_map_segment scope-resolved so it works in custom mode — is therefore REJECTED outright: adjust is a CV nudge tool, and the custom system has its own editing path (set_custom_segments replaces polygons wholesale). ONE RESIDUAL, tracked separately and NOT part of these findings: _handle_get_map_segments applies image_segment_adjustments to scope['segments_store'] with no mode check (mapping_services.py:1210), so in custom mode it consults a CV-only store. Inert today because the namespaces differ; it is a latent aliasing seam and a one-line guard, not the defect filed here."
 },
 {
  "finding_id": "#9:A3-REC-3",
  "adjudicated_at": "2026-08-05",
  "verdict": "does not reproduce",
  "reason": "THE BLOCKING RUN HAPPENED. It was held REOPEN pending 'a fresh group-phase hardware run to confirm the card no longer pins to room[0]'; job_2026-08-04T19-37-52 is that run, and it contradicts the reopen premise at every level.\n\nCLAIM: completed_room_ids is ALWAYS [] on a phased job, so live progress freezes on the group's first room and a false 'stuck' event fires.\nOBSERVED: parent [5, 8, 9], phase0 [5], phase2 [8, 9] — fully populated. The card also advanced room-by-room INSIDE phase 2 (Entryway struck through while Home Office ran at 72%), which is precisely the behaviour the reopen said was impossible.\n\nCLOSED AS DOES-NOT-REPRODUCE, NOT AS 'THE AUDIT WAS WRONG'. It was written 2026-07-30 and was very likely true then. The probable fixer is RP-047(a) (6831ccd, deployed 2026-08-03), which made the snapshot present the phase. Distinguishing 'never real' from 'real and since fixed' matters: the first would impugn the audit, the second is the audit working.\n\nTHE EXPENSIVE COROLLARY. RP-047(b) was built ON this finding's stale premise — that record_completed_room never fires inside a group dispatch — and marked every room in a phase 'current'. Completions DO fire, so that was strictly LESS accurate than what shipped, and it was reverted (45c18e7). Built from a NOTE instead of an OBSERVATION, and the note predated the fix that invalidated it. That is this campaign's most expensive stale-ledger lesson and the reason _verify_ledger.py exists.\n\nCONSEQUENCE STILL OPEN — RE-READ #?:A2-CAN-2. The audit recorded that A3-REC-3 is what made A2-CAN-2's under-reporting UNCONDITIONAL rather than occasional. With this premise gone, CAN-2's failure mode has changed and must be re-read against current code rather than against its ticket. Do not scope CAN-2 from its original text."
 },
 {
  "finding_id": "#18:A2-POLYGO-6",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: custom_components/eufy_vacuum/mapping/segment_primitives.py:341-370. The defective mechanism was the docstring claim 'Range 0-1; 1 = circle'. That string is gone. The current docstring states the opposite and correctly: 'RANKS BLOCKINESS, NOT ROUNDNESS — and the range is NOT 0–1' (:344), 'the attainable maximum is **π/4 ≈ 0.785**, not 1.0 — reached by any axis-aligned square, and by a single pixel' (:349-350), 'a digital disc of radius r has the SAME staircase perimeter as its bounding square (8r), so it scores π²/16 ≈ 0.617 — strictly LOWER than a square' (:352-354), plus a DO-NOT-normalise warning citing the real tuned thresholds (`< 0.08` fragmented floor, `>= 0.22` clean-room bar at adapters/eufy/segmentor.py:97/:902) at :360-363. The arithmetic itself is unchanged (`4πA/P²` at :370), which is what the finding asked for — the finding was documentation-vs-math, not a math bug. An adapter author reading this can no longer pick unreachable 0.8-1.0 thresholds.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "#18:A2-POLYGO-7",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: custom_components/eufy_vacuum/mapping/segment_primitives.py:548-576. The dead Rec.709 luminance step is physically removed from the body — the whole function is now three lines: `rgb_float = rgb.astype(np.float32) + 1.0` (:572), `channel_sum = np.maximum(rgb_float.sum(axis=2, keepdims=True), 1e-6)` (:575), `return rgb_float / channel_sum` (:576). No weighted-luminance array, no divide-by-L pass, and grep for 'Rec.709'/'luminance' in the file hits only docstring prose (:555, :562), never code. The docstring now names the cancellation explicitly — 'THERE IS DELIBERATELY NO LUMINANCE WEIGHTING' (:554) and '(rgb/L) / (sum(rgb)/L) == rgb / sum(rgb)' (:558) — and redirects a porting author to the clustering (adapters/eufy/segmentor.py:435-438, :553-555) instead. Both halves of the finding (maintenance trap + wasted per-pixel divide) are gone.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "#18:A3-IMAGE--4",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: Invalidation now exists on the write side and the analyze cache gate honours it. Write side: custom_components/eufy_vacuum/mapping/mapping_services.py:931 calls `_mark_segments_stale_for_variant(map_bucket, variant)` inside `_handle_upload_map_image` immediately after the `image_variants[variant]` row is written and BEFORE the art/backdrop branches; the delete path mirrors it at :1105 and surfaces `segments_stale` in its result (:1122). The helper is at :977-1002 — it no-ops for non-CV variants via `_CV_SOURCE_VARIANTS = frozenset({'dark','default','light'})` (:974, :994), marks rather than deletes (`segs['stale_since'] = utc_now_iso()`, :1001, preserving RP-006's zone/layout anchors), and is idempotent (:999-1000). Read side: the cache-hit gate is now `if not force_reanalyze and _cached.get('available') and not _cached.get('stale_since')` (:1208), so a default analyze after an upload falls through to a real re-run; the fresh engine result is assigned wholesale at :1292, so `stale_since` self-clears. The missing staleness signal is also closed: `_build_segments_response` sets `response['segments_stale'] = bool(base.get('stale_since'))` (:1176) and get_map_segments does the same at :1442. Tests exercise the helper (tests/unit/test_mapping_services_helpers.py:269-306, tests/integration/test_mapping_services.py:1350-1360).\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "agent: sensor (2-lens verified):SN-4",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: Seam is now the unified per-room sync `_sync_dynamic_entities` in custom_components/eufy_vacuum/sensor/__init__.py (the two byte-identical history/rule-status twins the finding called \"both sync blocks\" were merged into it; the twins are now thin wrappers at :408-434). The described mechanism — build a fresh entity per desired room, then DISCARD it when the unique_id is already known and push only a state write — is gone. At sensor/__init__.py:366-391 the loop now does: `existing = entity_dict.get(unique_id)`; `if existing is not None:` -> `if existing.room_name != entity.room_name:` -> replaces the dict slot with the freshly built object (`entity_dict[unique_id] = entity`) and schedules `_swap_renamed(_old=existing,_new=entity)` (:382-386) which awaits `_old.async_remove()` then `async_add_entities([_new])`, `continue`-ing PAST the `_request_entity_state_write(existing)` fallthrough at :388. Only the unchanged-name path still does the bare state write. The comparison key is real: `EufyVacuumRoomEntity.room_name` is a property at custom_components/eufy_vacuum/room_entities.py:79-87 returning `self._room_name`, which is captured once in `__init__` at room_entities.py:38 and fed to `_attr_translation_placeholders = {\"room\": self._room_name}` at room_entities.py:53 — so a rebuilt sibling genuinely carries the new name and the swap re-resolves the friendly name at add-time. The fix deliberately does NOT call `registry.async_update_entity(name=...)` (comment at :377-379, to avoid stomping a user override), so the finding's \"async_update_entity has ZERO occurrences\" observation is still true and is no longer the test. Regression-locked by tests/integration/test_init_setup.py:569-618 `test_room_rename_swaps_entity_keeping_unique_id`, which asserts `after_entity is not before_entity` and `after_entity.room_name == \"Great Room\"` while the registry entity_id for unique_id `vacuum_alfred_6_1_cleaning_history` is unchanged — i.e. it rejects a mere state re-write on the stale object. Same fix shape also present in button.py:99-117 and number.py:132-150. SCOPE BOUNDARY / sibling gap outside SN-4's declared seam: custom_components/eufy_vacuum/switch.py:90-96 still has the ORIGINAL pattern — `if existing is not None: existing.async_write_ha_state()` with the rebuilt `EufyVacuumRoomEnabledSwitch` (same EufyVacuumRoomEntity base, same placeholder name capture, built at switch.py:61-68) discarded and no room_name comparison. That is the SN-4 mechanism surviving on the switch platform; it does not change SN-4's verdict at sensor/__init__.py but should be filed.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "agent: sensor (2-lens verified):SN-9",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: Seam: `EufyVacuumMapOverlaysSensor.native_value` in custom_components/eufy_vacuum/sensor/map_overlays.py. The finding's exact source line `if not res.get('present'): return 'unavailable'` no longer exists — map_overlays.py:85-89 now reads `def native_value(self) -> str | None:` / `res = self._result()` / `if not res.get(\"present\"): return None`. The recommended alternative was adopted verbatim: a real `available` property was added at map_overlays.py:72-82 returning `bool(self._result().get(\"present\"))`, with a docstring naming SN-9 and stating the reason is carried in `extra_state_attributes[\"reason\"]` rather than encoded into the state string; that attribute is still populated on the not-present branch at map_overlays.py:109-111. So not-present now flows through HA's own availability mechanism (available=False + native_value None) instead of a literal string colliding with the reserved state. Repo-wide check: `grep -rn 'return \"unavailable\"' custom_components/` returns ZERO hits, so the sentinel is not lurking elsewhere in the integration. Regression-locked by tests/unit/test_map_overlays_sensor.py:53 ([MOS-1d] \"absent map_state_source -> available=False, native_value=None\") and tests/integration/test_sensor_map_overlays.py:185 (\"SN-9: not-present is reported via the REAL HA availability mechanism\"). Note, not a residue of this finding: native_value still returns the literal string \"available\" at map_overlays.py:92 when the map IS present but current_room is None. \"available\" is not an HA-reserved state (only \"unavailable\"/\"unknown\" are), so the described collision does not apply, though the wording is confusing next to the availability property.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "direct read:DR-ONB-2",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: custom_components/eufy_vacuum/onboarding/manager.py:277-284 — check_for_new_rooms now imports get_active_map_id from ..rooms.room_discovery and returns False when `active_map_id is not None and str(active_map_id) != str(map_id)`, BEFORE reading the un-scoped live attribute at line 286 (`source_state.attributes.get(attribute)`) and comparing it to `map_ob[\"room_count_at_last_check\"]` at 291. The described mechanism — a per-map stored count compared against the ACTIVE map's live segment list on a non-active map — can no longer execute: the cross-map comparison is refused instead of answered. Docstring at 237-259 names DR-ONB-2 and states the design (unknown active map falls through unchanged, which is the deliberate single-map/boot-window behaviour, not the defect). Backed by tests/integration/test_onboarding_manager.py:98-136 [OB-3b], which monkeypatches get_active_map_id to \"OTHER_MAP\" (expects False), to _MAP (expects True), and to None (expects True). get_active_map_id itself is real at custom_components/eufy_vacuum/rooms/room_discovery.py:43.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "live:ENT-1",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: The derivation itself is unchanged (custom_components/eufy_vacuum/adapters/eufy/entities.py:125-126 still does `vacuum_entity_id.split(\".\",1)[-1] + suffix`), but the defect's mechanism — no device-registry lookup and no fallback, so an oddly-named companion makes the role read absent — is repaired at the resolution seam. custom_components/eufy_vacuum/core/capabilities.py:182-269 defines augment_candidates_from_device: it looks up the vacuum in `er.async_get(hass)`, takes `er.async_entries_for_device(registry, entry.device_id, include_disabled_entities=True)`, learns the suffix off each declared candidate that starts with the vacuum's object_id, and appends siblings whose DOMAIN matches and whose object_id ends with that suffix (line 263), leaving the derived id first in the list. It is WIRED, not just present: capabilities.py:310 `_cands = augment_candidates_from_device(hass, vacuum_entity_id, entity_candidates)` and every `_find`/`_find_reg`/`_any_present` at 313-323 reads `_cands`, which is what feeds water_level -> supports_mop_features (capabilities.py:388-390) -> supports_water_control (411). Degradation paths return the input unchanged (lines 217/223/232/234), so a fallback can never be removed. The maintainer's area-prefix case is covered end-to-end by tests/integration/test_core_capabilities.py:469-493 [CAP-11e] (device exposes sensor.downstairs_alfred_task_status only; detect_capabilities must resolve it), plus CAP-11/11b/11c/11d at 378-466. The sibling pattern path (live-map camera) was separately hardened at core/manager.py:5396-5403 using the same helper (that one is ENT-2).\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "agent: platforms (2-lens verified):EP-4",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: custom_components/eufy_vacuum/number.py:215-217 — EufyVacuumMaintenanceIntervalNumber now declares `_attr_should_poll = False`, and number.py:278 — async_set_native_value ends with `self.async_write_ha_state()` after `await self._manager.async_save()`. Both halves of the described defect are gone: the class no longer depends on the platform's 30s SCAN_INTERVAL to publish a value it mutated in storage, so the 30s lag after a card-side interval save is gone and adding should_poll=False can no longer freeze the entity. The module comment at number.py:7-11 was rewritten to match the code (\"each explicitly sets _attr_should_poll=False ... rather than relying on HA's default polling to paper over a missed async_write_ha_state() call (EP-4)\") above PARALLEL_UPDATES = 0, so the comment is now a true claim rather than the false one the finding flagged. Sibling classes confirmed poll-free: room_entities.py:20 (base of EufyVacuumRoomOrderNumber), button.py:235, binary_sensor.py:67, sensor/*.py. Pinned by tests/integration/test_number_entity.py:138-155 [NE-7] (patches async_write_ha_state, asserts called once) and :158-162 [NE-9] (asserts _attr_should_poll is False).\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "#18:A3-IMAGE--8",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: custom_components/eufy_vacuum/mapping/mapping_services.py:71-95 adds `_png_dimensions(data)`, a dependency-free IHDR parse (magic check + `data[12:16]==b\"IHDR\"`, width/height from bytes 16:24), so dimensions are measurable with no Pillow. In `_write_and_measure` (same file, :860-909) the source ladder is now IHDR -> Pillow -> declared: `actual_width, actual_height = _png_dimensions(image_bytes) or (None, None)` at :875, Pillow only as the second try at :877-883. The described mechanism (persist None dims AND report saved:True) is gone twice over: :885-897 refuses with `{\"saved\": False, \"reason\": \"unreadable_image_dimensions\"}` and `os.remove(file_path)` when either dim is falsy, and the variant row at :920-927 (`variants[variant] = {... \"width\": result[\"actual_width\"], \"height\": result[\"actual_height\"]}`) is only reached under `if result[\"saved\"]:` at :913 — so a null-dimension variant row can no longer be written.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "#18:A4-CUSTOM-2",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: `_resolve_active_scope` (custom_components/eufy_vacuum/mapping/mapping_services.py:1773-1811) still returns throwaway literals in the custom-mode-no-layout branch (:1799-1804) but now tags every return with a `resolved` flag (True at :1792 and :1806, False at :1801). Both writers named in the finding check it before mutating: `_handle_set_segment_room_link` at :2084-2087 (`scope = _resolve_active_scope(map_bucket); if not scope[\"resolved\"]: return {\"saved\": False, \"reason\": \"no_active_custom_layout\"}` — the `links` binding at :2087 is only reached past the guard) and `_handle_set_companion_anchor` at :2146-2149 with the identical guard before `anchors: dict = scope[\"anchors\"]`. The only other two call sites are read-only: :1320 in the get_map_segments handler (reads `scope[\"segments_store\"]`) and :1496 in set_segmentation_mode (counts segments for the response). So no write path can mutate a garbage-collected dict and still return `saved: True`.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "#10:A5-AG-2",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (medium confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: The \"no shipped surface names the offending room\" mechanism is gone end to end. New `AccessGraphManager.access_graph_block_rooms(managed_rooms, validation)` at custom_components/eufy_vacuum/rooms/access_graph.py:1069-1126 walks the raw validation issues' `room_id`/`rooms` fields, dedups, and returns name-sorted `[{room_id, name}]`. It has real callers, not just a definition: planning/run_plan.py:1084-1087 calls it via the manager facade (core/manager.py:2581-2586) and at :1089-1100 the refusal message becomes \"Room access is incomplete for <names>...\" (the old room-less sentence survives only as the empty-list fallback), with :1119-1123 adding `reason_params={\"rooms\": [...], \"room_ids\": [...]}` and `blocking_rooms`; core/manager.py:1947 also returns `blocking_rooms` from set_room_access_graph. Card side: src/state/rooms.js:802-806 `_blockDescriptor` now carries `params: source?.reason_params`, src/renderers/rooms.js:136-145 feeds it to `resolveCodedLabel`, and src/state/coded-label.js:55-65 joins the list param with the locale separator against `rooms.block_reason.incomplete_access_graph` = \"Room access is incomplete for {rooms}...\" (src/i18n/en.js:1310, present in all 18 locale files). RESIDUAL, flagged deliberately: the other half of the title — one inbound-less room still flips the whole graph to `partial` and hard-blocks every run — is unchanged (`_access_graph_state` :1036-1040 returns \"partial\" -> `access_graph_block_code` :1063-1064 returns \"incomplete_access_graph\"), and the docstring at :1088-1092 states that semantics change was deliberately left to Chris. Hence medium, not high: the actionability defect described in the impact is fixed, the map-wide block scope is not.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "live:DIAG-1",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: custom_components/eufy_vacuum/diagnostics.py:383-440 adds `_device_entity_census`, which resolves the vacuum entity in the entity registry (`er.async_get(hass).async_get(vacuum_entity_id)`), takes its `device_id`, and enumerates real siblings via `er.async_entries_for_device(registry, device_id, include_disabled_entities=True)` -> emits `entity_count` plus a sorted `entities` list of `{entity_id, disabled, platform}`. It is wired into the dump at :471 (`out[\"device_entities\"] = _device_entity_census(...)`) alongside `out[\"entity_resolution\"]` at :467, and :490-503 adds `entity_resolution_summary` with `declared`, `unresolved`, `device_entity_count`, and `likely_naming_mismatch = bool(unresolved and isinstance(sibling_count,int) and sibling_count > 0)`. Reachability confirmed: `async_get_config_entry_diagnostics` (:759) fans out to `_vacuum_diagnostics` at :811. The described mechanism -- 'the dump never lists what the device actually exposes, so a naming miss and an absent capability are byte-identical' -- is gone: a naming miss now reads as unresolved roles WITH a populated sibling census, an absent capability as unresolved roles with `entity_count: 0`. Degenerate cases are named rather than blank (`vacuum_entity_not_in_registry`, `vacuum_entity_has_no_device`). Introduced by commit 20c0ab1 'fix(diagnostics): live:DIAG-1'.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "direct read:DR-ONB-1",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: custom_components/eufy_vacuum/onboarding/manager.py:214-229 no longer mutates while iterating. It takes an immutable snapshot first -- `before = dict(confirmed)` (:215) -- normalizes the remap to string keys (:216), builds a NEW dict `rebuilt` from keys not named as an old_id (:218-220), then writes `rebuilt[new_key] = True` only from `before.get(old_key)` (:221-223), so every read is of the OLD state. It finishes with `confirmed.clear(); confirmed.update(rebuilt)` (:228-229) to preserve object identity for callers holding a reference. Traced the record's own reproducer by hand: before={'1','2','3'} all True, remap {1:2,2:3,3:4} -> rebuilt {'2':T,'3':T,'4':T} (was {'4':True}); swap {1:2,2:1} with only room 1 confirmed -> {'2':True}, not the wrong-room {'1':True}. Both shapes are now pinned by tests/integration/test_onboarding_manager.py:200-232 ([OB-6b] chain, [OB-6c] swap) and identity by :246. Sole production caller rooms/room_crud.py:330 is unchanged, so the fixed function is the one actually reached.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "#10:A6-AGX-5",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: Seam is `get_room_access_editor` in custom_components/eufy_vacuum/rooms/access_graph.py. The per-room membership test is now `_issue_applies` at :744-748 — `room_ids = [str(v) for v in list(issue.get(\"room_ids\", []) or []) if v is not None]; return not room_ids or str(room_id_int) in room_ids`. The `not room_ids` disjunct is the fix: a graph-scoped issue with an empty room_ids list now reaches EVERY room's editor. `missing_dock_room` — the exact issue the finding said was never surfaced — is formatted at :419-425 with `\"room_ids\": []`, so it passes. `context[\"issues\"]` is the FORMATTED list (built at :508-512, returned at :546), so the shapes line up. The `is not None` filter additionally defends against the literal None that the multiple_inbound branch used to put in room_ids (:407-416). Covered by tests/integration/test_access_graph.py:535-546 [AG-11b], which seeds a map with grants and NO dock room and asserts `out[\"issues\"]` is non-empty for room 2 (could not execute: pytest-homeassistant needs fcntl, unavailable on Windows — verdict rests on the code read). Note the deliberate residual documented at :750-754: `multiple_dock_rooms` names only the dock rooms so an ordinary room still sees nothing for it — that is a different issue type from the one this finding named.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "#10:A6-AGX-6",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: The logic moved out of src/state/room-access.js (which at :88-94 now just builds `withEdges(graphFromRooms(rooms), room.id, draft)` and delegates) into src/state/access-graph-model.js `offerableTargets` :183-223. The defect was: dock room filtered out of `known`, then every selected id not in `known` got appended as a synthetic \"Missing Room N\". Current filter at :196 is `if (dockRoomIds.has(id) && !selectedIds.has(id)) return false;` — the dock room is excluded ONLY when this room has not already selected it. An existing edge into the dock therefore stays in `known` and is emitted at :199-206 with its real `name`, `missing: false`, `available: true`, so it renders under its own name and can be clicked off. The synthetic missing list at :208-210 is built from `selectedIds` minus `knownRoomIds`, so the now-present dock room cannot also appear as a ghost. Verified by execution: `node --test src/state/room-access-logic.test.mjs` → 15/15 pass, including [RAC-10] \"a dock room already selected by this room stays visible\" (asserts missing===false, available===true, name===\"Room 3\"), [RAC-11] no duplicate synthetic entry, and [RAC-12] an UNSELECTED dock room is still not offered (the fix is scoped to preserving an existing edge).\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "#18:A5-FURNIS-4",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (medium confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: The described mechanism (dragged area label in a map-level `area_label_anchors` side-table keyed by device room id, unpruned on rebuild) is gone. The anchor now lives ON the room record: `_LABEL_ANCHOR_KEY = \"label_anchor\"` at custom_components/eufy_vacuum/mapping/mapping_services.py:1644; the write path `_handle_set_area_label_anchor` calls `_migrate_area_label_anchors(map_bucket)` (mapping_services.py:2478, write-path only) then sets `room[_LABEL_ANCHOR_KEY] = anchor` (2509), falling back to the side-table only for rooms with no managed record; `resolve_area_label_anchors` (1647-1677) derives the wire dict from the rooms with the room-record value winning over any legacy entry. Carry-forward is real: rooms/reconciliation.py:274 `carried = dict(source)` re-keys the whole record to the new id after slug matching, so the anchor rides a re-import. RESIDUAL, and the reason for medium: the impact's last sentence extends the finding to `companion_anchors` and each custom layout's furnished `rooms` map — those are still id-keyed dicts (mapping_services.py:1809 `map_bucket.setdefault(\"companion_anchors\", {})`, layout `rooms` at 1830) and the apply-reconciliation path (rooms/room_crud.py:310-335) remaps only rule-status snapshots and floor-type confirmations, never those two. Nothing in custom_components/ prunes or remaps companion_anchors (grep: only mapping_services.py + const.py). So the titled seam is fixed; the two siblings named in the impact are not, and no other finding in _open_findings.json/_direct_reads.json covers them.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "#8:A5-PP-RP-2",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: The refusal is now gated on a whole-plan clean count, not phase 0. custom_components/eufy_vacuum/jobs/job_monitor.py:296-303: `_nothing_to_clean = int(clean_phase_count) <= 0 if clean_phase_count is not None else int(payload_room_count or 0) <= 0` before returning reason `invalid_payload` / \"Room-clean payload is missing or invalid.\" The only caller, custom_components/eufy_vacuum/core/manager.py:3535-3552, computes `_clean_phase_count = sum(1 for _phase in _plan_phases if room_count > 0 or (_phase.get(\"phase_type\") == \"zone\" and _phase.get(\"zones\")))` over `start_plan[\"phases\"]` and passes it. Zone phases are built with `room_count: 0` at planning/run_plan.py:938-944, which is exactly what used to trip the old `payload_room_count <= 0`. Breaks (charge_wait/wait, run_plan.py:915-925) carry room_count 0 and no zones so they cannot keep a plan alive. The `_plan_phases = [payload_state]` fallback (manager.py:3533) still counts a zone-first payload correctly. grep confirms `build_start_blocker_from_lifecycle` has exactly one call site and `invalid_payload` is emitted only at job_monitor.py:302. Tests JM-25b/25c/25d at tests/unit/test_jobs_job_monitor.py:317-348 pin it. Noted, not the finding: manager.py:3506 still refuses with `all_selected_rooms_blocked` when every selected room is blocked even if a zone remains — different reason string, and it names the blocked rooms, which is what the finding asked for.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "#10:A6-AGX-1",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: get_access_graph_health now emits a verdict. custom_components/eufy_vacuum/rooms/access_graph.py:780-825 returns, in addition to dock_room_ids/missing_rooms/issues: `\"state\": self._access_graph_state(...)` (808), `\"runs_blocked\": block_code is not None` (809), `\"block_code\": block_code` (810), and `\"unlinked_room_ids\"` (816) — the rooms that would become missing_dependency the moment a dock room is set, which is precisely the trap the impact described (the report's own remediation moving blank -> partial). `_access_graph_state` (access_graph.py:1015-1040) returns blank/partial/complete, and `access_graph_block_code` (1043-1067) is the single owner of \"do runs block?\", consumed by the start gate at planning/run_plan.py:1066-1075 (`_graph_block_reason = self._manager.access_graph_block_code(managed_rooms, validation)`), so the diagnostic and the gate cannot disagree. Blank and partial are therefore no longer byte-identical: blank yields runs_blocked False (unless rooms have rules -> access_graph_required_for_rules), partial yields runs_blocked True / block_code incomplete_access_graph. Tests AG-10b/AG-10c at tests/integration/test_access_graph.py:574 and 605 assert the blank-vs-partial distinction and the strand list.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "agent: platforms (2-lens verified):EP-5",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: custom_components/eufy_vacuum/button.py:231-262 — EufyVacuumSavedRunProfileButton no longer overrides `name`: grepping lines 231-420 of the class returns no `def name`, no `_attr_name`, and no `f\"Run {...}\"` f-string. It now declares `_attr_translation_key = \"run_profile\"` (line 242) and `self._attr_translation_placeholders = {\"profile\": self._profile_name}` (line 262), i.e. the same mechanism as the sibling EufyVacuumMaintenanceResetButton (`_attr_translation_key = \"maintenance_reset\"`, line 198). The translation target exists: custom_components/eufy_vacuum/strings.json `entity.button` = {\"maintenance_reset\": {\"name\": \"Reset {component} Maintenance\"}, \"run_profile\": {\"name\": \"Run {profile}\"}} and translations/en.json carries the same `run_profile` key (I parsed both files; the translations dir holds only en.json, which is the pre-existing HA-backend shape — the 18 packs are card-side i18n, not entity names). The one residual English literal is line 261's `or \"Saved Run\"`, but that is now only a fallback VALUE for an empty user-supplied profile name substituted into {profile}, not the entity name template — the verb \"Run\" is resolved by HA from strings.json in every locale. No EP-5 marker was needed for this call; the class comment at 237-241 does cite it, and the mechanism it describes is gone.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "agent: platforms (2-lens verified):EP-7",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: custom_components/eufy_vacuum/room_entities.py:99-174 — branch 2 no longer returns unconditionally. Lines 122-131 build BOTH `managed_updates` (keys in managed_field_names) and `remaining_updates` (keys not in it). Line 132-142: `if managed_updates:` calls update_room_fields(**managed_updates), then `if not remaining_updates: save; write_ha_state; return` — the early return is now GUARDED on there being nothing left. Otherwise control falls through to the generic merge, where line 157 does `current.update(remaining_updates if managed_updates else updates)`, followed by the summary rebuild at line 163 (`build_room_selection_summary`), `_refresh_room_derived_state` (164), `_notify_rooms_updated` (168), save and write state. The finding's exact repro — `_async_update_room({'enabled': True, 'color': '#ff0000'})` — now persists both keys. Verified `current` is not clobbered: room_entities re-reads the same live dict object that update_room_fields wrote to (core/manager.py:1663 `rooms[room_key] = updated_room`, same map_bucket via ensure_map_bucket/setdefault). The sub-claim about the skipped summary rebuild is also moot: core/manager.py:1740-1741 shows update_room_fields itself sets `map_bucket[\"summary\"] = build_room_selection_summary(...)` on its success path. Regression test: tests/integration/test_platform_files.py:125-147 `test_room_update_mixed_managed_and_unmanaged_fields` asserts color reaches storage while only `enabled` reaches update_room_fields.\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "#10:A6-AGX-2",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: Seam is `RoomManager.update_room_fields` in custom_components/eufy_vacuum/core/manager.py (now at :1549, gate at :1645-1739). The gate is no longer absolute: :1656-1661 captures `baseline_keys = {structural_issue_key(i) for i in self._structural_access_graph_issues(self._validate_room_access_graph(managed_rooms=rooms))}` BEFORE the mutation (`rooms[room_key] = updated_room` at :1663), re-validates at :1665, then :1671-1674 filters to `new_structural_issues = [i for i in structural_issues if structural_issue_key(i) not in baseline_keys]` and only :1718 `if new_structural_issues:` rolls back and returns error `invalid_access_graph`. So a pre-existing violation is in the baseline and cannot reject an unrelated fan-speed/enable/colour edit. Identity helper `structural_issue_key` is at rooms/access_graph.py:43-74 and includes `source_room_ids` (sorted) and unsorted `rooms`, so \"made an existing violation worse\" / a new cycle over the same rooms still yields a fresh key and is still rejected. This is the ONLY such gate: `grep invalid_access_graph` hits only manager.py:1732/1733 and 1899 (set_room_access_graph), and `def update_room_fields` exists only at manager.py:1549 plus the thin service wrapper services/rooms.py:278. Residual (not the finding's mechanism): the reason_detail literal \"The requested access links would make the graph invalid.\" is still used even when the new violation came from an is_dock_room toggle rather than a link, but the response now carries the specific `issues` list (:1735-1738) and the card renders those, not reason_detail.  Note also the A5-DOCK-1 no_dock_room gate at :1694-1716 is deliberately NOT delta-scoped but is confined to `grants_access_to is not None` + links actually changed, so it does not re-introduce the blanket refusal on colour/fan edits. Tests encoding the behaviour exist at tests/integration/test_access_graph.py:473 [AG-8c], :508 [AG-8d] and tests/integration/test_manager_rooms.py:800 [MR-10e], :821 [MR-10f] (could not execute — pytest_homeassistant_custom_component imports `fcntl`, unavailable on this Windows host).\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "#10:A6-AGX-3",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: Seam is `AccessGraphManager.get_room_access_editor`, custom_components/eufy_vacuum/rooms/access_graph.py:557-778. Selectability is now delta-scoped with the same helper as A6-AGX-2: :610-613 hoists `baseline_keys` out of the candidate loop from `self._structural_access_graph_issues(context[\"validation\"])`; per candidate target :633-643 builds the candidate graph, validates, then keeps only `new_issues = [i for i in candidate_structural_issues if structural_issue_key(i) not in baseline_keys]`, and only `if new_issues:` (:644) sets `selectable = False`. A pre-existing multiple_dock_rooms / stored multiple_inbound / reconciliation-made cycle is therefore in the baseline and greys out nothing. The contentless reason is also no longer the common path: `_names_edge` (:647-668) matches an issue against BOTH endpoints (room_id, target_room_id, rooms[], source_room_ids[]), and :682-694 maps cycle_detected/duplicate_edge/missing_room/self_reference/multiple_inbound to specific reasons plus a `reason_code` (would_cycle / already_linked / target_unavailable / self_link / target_has_inbound); \"Not selectable due to graph legality.\"/`graph_illegal` survives only as the unreachable else at :695-701. `reason_code` is emitted on each target at :714. Test [AG-13b] tests/integration/test_access_graph.py:549-570 seeds two dock rooms and asserts room 4 still has a selectable target (not run — fcntl import failure on Windows).\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 },
 {
  "finding_id": "#10:A6-AGX-4",
  "adjudicated_at": "2026-08-05",
  "verdict": "verified applied at source (high confidence)",
  "reason": "BACKFILL OF A HAND-TICK THAT HAD NO DATA BEHIND IT. This finding was ticked in OPEN-FIX-CHECKLIST.md during the campaign but never written back to the generator's inputs, so regenerating the file would have silently discarded the verification. Re-established against current source 2026-08-05 rather than trusted.\n\nEVIDENCE: Backend still composes English `message` in `_format_access_graph_issue` (rooms/access_graph.py:316-480) — deliberately, as the documented response-service surface (docstring :326-332) — but every branch now also returns `code` + `params` with values unjoined (e.g. multiple_inbound at :403-417 returns params {room, sources: list}; cycle_detected :385-392 params {rooms: list}). The card no longer renders `message`: the modal issue list is src/renderers/room-access.js:148 `accessIssueLabel(issue, (k,v)=>this.t(k,v))` and the save-error banner is src/bindings/room-access.js:121-128 `result.issues.map(issue => accessIssueLabel(...))`. Resolution is src/state/access-issue-label.js:41-51 -> src/state/coded-label.js:83-110, which tries `room_access.issue.card.<code>` then `room_access.issue.<code>`, joins list params with a locale separator (listSeparator, :36-45) and only falls back to `message` when the code misses. I verified the packs rather than trusting the wiring: all 21 `room_access.issue.*` keys used by en.js are present in all 17 non-English packs in custom_components/eufy_vacuum/frontend/locales/*.json (ar, cs, de, es, fr, he, id, it, ja, ko, nl, pl, pt, ru, tr, zh-Hans, zh-Hant — 21/21 each, zero missing), with correct placeholders (de multiple_inbound = \"{room} erhaelt von mehr als einem Raum Zugang ({sources})...\") and a per-locale `list_separator`. Every code the backend can emit (self_reference, missing_room, duplicate_edge, cycle_detected, multiple_inbound, missing_dock_room, multiple_dock_rooms, missing_dependency, unreachable_from_dock, unknown_issue) has a key. The shipped bundle carries it too: custom_components/eufy_vacuum/frontend/eufy-vacuum-command-center.js contains both resolver prefixes (\"room_access.issue.\" and \"room_access.issue.card.\") plus all the concrete keys. These two are the only card surfaces that render access issues (no card consumer of get_access_graph_health exists — grep for graph_health/accessGraphHealth in src/ returns nothing). Residual outside the finding's scope: the save-error banner's `?? result.reason_detail` fallback (bindings/room-access.js:125) can still show English for refusals that carry no issues list (no_dock_room, room_not_found).\n\nMarker-independent by method: the check was whether the finding's described MECHANISM is still present, not whether the code cites its id. That distinction is why this is trustworthy — the 2026-08-04 pass used id-citation as its test and put two already-fixed findings back on the board."
 }
]