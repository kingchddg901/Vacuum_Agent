# eufy-vacuum-manager — engineering notes (migrated from central memory)

> **RESUMING / AFTER A CRASH → read the LIVE handoff FIRST — it is NOT in this repo:**
> `~/.claude/projects/--192-168-4-104-config-/memory/SESSION_HANDOFF.md`.
> This index holds durable facts; the handoff holds the CURRENT state (what is
> unpushed, what was mid-flight, what has already been ruled on).
>
> ⚠ **CORRECTED 2026-09-05:** [`SESSION_HANDOFF.md`](SESSION_HANDOFF.md) in THIS directory is
> **NOT a pointer stub any more — it is the LIVE handoff** (2,100+ lines, current). The central
> copy is stale (144 lines, last touched 2026-08-30). This warning was written when the local
> file really was a stub and was never revised when the situation reversed, so anyone following
> it went to the older state. Read the LOCAL one.
> The original hazard still stands as history: until 2026-08-15 there were two diverging
> handoffs of the same name and both indexes said "read SESSION_HANDOFF.md first", so which
> state you got depended on which index you opened.

> 💰 **Agent cost economics — measured, do not re-derive.**
> `dreame-port-fixture/authoring/telemetry/WHAT_THIS_ANSWERS.md` (data: `agent_runs.csv`,
> **7,537 runs**). Answers: what an agent costs before it does anything (~86k boot), what
> reading its files adds (+136k, 1.6x the floor), what the actual job adds (+10k, ~4%), and
> therefore when to batch small jobs. Measured by a proper spin-up ablation, not estimated.
> I re-measured all of this from scratch once because the filename does not say what it holds.

Local, **git-ignored** knowledge base for this repo — the deep technical detail
that used to live in Claude's central memory index. Read these when working the
corresponding subsystem; the central `MEMORY.md` keeps only a pointer here.

Same format as the central index: one tight hook line per file, detail in the
file. Grow freely — this is not size-capped.

## Audits
- ⏳ [**DRAFT rulings — the 18 `.py` rows that owe one**](DRAFT-rulings-py-walk.md) — **DRAFT ONLY: nothing ruled, minted, or declared.** Candidate wording for Chris on the 18 harvest rows classified bare `INVARIANT`. Every site re-read at SOURCE 2026-08-21, not from the harvest table's truncated Notice column — which is why two rows the handoff listed do not exist as bare-`INVARIANT` rows at all. ⚠ **CARRIES ITS OWN CORRECTION (2026-08-21, Chris: "we do not have to force them to be anything").** An initial grouping compressed 18 rows to 7 rulings and that compression WAS the error — the honest count is **~12–13**, and only Family D survives as a real family. **Test for a family: one sentence covers every member and no member needs a clause the others do not.** Also killed one of my instincts: single-site entries are NORMAL (33 `IN`s, ~1 planted `anchor:` site each), so site count is not the test for whether a row earns an entry. Each draft carries the input that makes it RED, per the bite test. ⚠ **Three carry collision flags against the existing 36** — the sharpest is Family B (*a result must carry the reason that produced it*) sitting next to `INT62M7A`/`IN5BRA39`/`IN2QDNB3`: `INT62M7A` is scoped to MUTATIONS and all five sites are READS, so this is plausibly its read-side sibling rather than a new token. ⚠ **One of the seven may not be an `IN`** — the stall-capture privacy default reads as a `PN`-class project principle (nothing in the 36 covers consent) and generalizes past this listener to any future capture/export/upload. **Family B is the code-level statement of the campaign's own unifying finding** (absence of a complaint mistaken for a pass), one layer down
- ⭐ [**LEDGER — defects, CODE vs DOC**](LEDGER-defects-code-vs-doc.md) — the `.py` walk's defect output, split per Chris's ruling that code defects and doc defects are tracked apart. ⚠ **THE ORDERING RULING REVERSED 2026-08-22: docs are written FIRST and every code defect a doc pass finds is DEFERRED** — see [REPAIR-BACKLOG.md](REPAIR-BACKLOG.md). The old rule read 'fixed BEFORE the authoritative docs are written'; it was inverted once the doc campaign became the critical path, and a doc pass now stays clean of code edits so a documentation commit never carries one. **~52 code / ~35 doc / 5 test holes, NONE FIXED.** Carries the EVIDENCE behind every harvest verdict — enforcement site, whether a test can make it red, whether one exists — so do not re-derive it. Highest-value open: **C35** learning teaches the COARSE duration (`build_room_stats_payload:481` reads `duration_minutes` where `room_cleaning_minutes` sits in the same record; VERIFIED on his store — 16 of 74 jobs, one-line fix, and the same file already prefers the refined value on two other surfaces); **C23** Lever B's fixture registers `SupportsResponse.OPTIONAL` where upstream Roborock registers `ONLY`, so `[LRR-1]` asserts against the TEST FILE's own dict and the suite stays green while Lever B sticky-disables on his box; **C16** a HANG not a crash (`_norm_to_px` filters NaN and not `inf`; `while d < seg` never terminates, on an executor thread); **C1/C22** `button.py:84` deletes registry entries by unique_id PREFIX — the one mechanism `IN4CW5Y9` forbids; **C28** onboarding remap purges SOURCES never TARGETS, so a room reads `confirmed` the user never confirmed → start gate opens → mop on unverified carpet
- 🧪 [**PRE-REGISTRATION — does naming a defect shape manufacture it?**](_PREREG-partial-guard-priming.md) — predictions committed in writing BEFORE the blind run, scored after. **Answer: priming STEERS, it does not inflate.** Told to look for partial guards, agents found partial guards — but a blind re-read of the same file found a comparable number of *entirely different* defects the primed run missed. Non-overlapping in BOTH directions on two same-file pairs; **neither dominates**. **The rule: what you do not name, you do not get.** A single prompt shape is a COVERAGE limitation at least as much as a bias risk. The two lenses that actually split are **INWARD** (control flow, guard completeness) and **OUTWARD** (verify every claim against the commit it cites, the consumer it names, the data on disk) — blind agents wander outward by default. Chris's ruling: run BOTH, union is the yield, disagreements are flags
- 📓 [**HANDOVER — the 2026-08-13/14 mobile pass**](HANDOVER-mobile-pass-2026-08-14.md) — **HISTORICAL, not current state** (was `SESSION_HANDOFF.md` here until the 2026-08-15 rename that closed the duplicate-filename trap). Kept because NONE of its content is in the central handoff — §5 there is overwritten per work block and this pass was never merged in. ⭐ **The live item is the TRAIT SCORER design (AGREED DIRECTION §2), still UNBUILT** — score device entities on registry traits (device_class, unit ft²/m² AND not the token `total`, select options, state vocabularies) instead of generating names; it is the only thing that can separate the `cleaning_area` / `total_cleaning_area` collision, which name matching provably cannot. Core owns the traits, each brand supplies only its vocabulary. ⚠ Item 3 (**user entity override**) has since SHIPPED — `entity_overrides` is live. Also holds: the 65/65 stem finding (one override generalises to a whole device), the recommended precedence (override WINS but FALLS THROUGH), the landscape shell rule (**narrow OR short** — width alone rendered desktop chrome on a rotated phone), two defects with reach (animal SVGs consume bare HSL components while themes store 8-digit hex → black fills for every user who set one; hardcoded `" %/min"` wrong in NINE locales), the locale-width tooling (**character count picks the wrong languages**), the OpenDyslexic licence work, and Chris's rulings not to re-litigate
- 📕 [**PROTOCOL — the doc reconciliation pass**](PROTOCOL-doc-reconciliation-pass.md) — **THE SINGLE SOURCE for how a reconciliation pass is run.** PRIVATE (notes, git-ignored — Chris: "this document's for you and me"), never move it under `docs/`. Consolidated 2026-08-13 from three diverging copies (central-memory recipe `project_doc_drift_audit` + `TASK-doc-drift-2026-08-06` §Method + `TASK-write-audit-methodology` raw material) — multiple copies of a method IS the drift, per 00 §5.4. ⚠ **THE EPISTEMIC INVERSION** — the code audit reads the doc for INTENT (doc-first); a reconciliation pass puts the doc **on trial**, so never resolve a disagreement by consulting the doc under test. **Pin the ref (v2.0.0 worktree), SOURCE-ONLY — never patch a doc from a commit message.** Regen scripts BEFORE agents; fan out by AREA; **fix one agent per DOC FILE = the conflict-free decomposition**; batch ~5 (16-wide tripped a burst limit); gate `mkdocs --strict`. **Reconstruction-diff beats read-through for OMISSIONS** and doubles as the DR drill. Hazards: a fix-agent WILL gut a preserved historical section (banner it); NEVER `git checkout` a doc mid-verify; reader-without-producer = DORMANT not deleted. Calibration + model fit (Fable wide-read / Sonnet per-artifact) §13
- ⏳ [**TASK — doc drift from 2026-08-06**](TASK-doc-drift-2026-08-06.md) — OPEN, piecemeal one subsystem per pass. ⚠ **Framed in DR/epoch terms, both RETIRED 2026-08-16** (`f5408d9e`) — read it as plain doc drift; there is no baseline to fold into any more, a stale NOW doc is simply corrected. 154 commits landed inside DR-graded subsystems; ~~`23-error-tracker.md` is PROVEN confidently-wrong~~ — **FIXED by `5940830`**; the doc now says `int | str | None`, matching `_code_key`. ⚠ THE INDEX SAID OTHERWISE FOR DAYS: the note's own banner recorded it DONE while this row still called it proven-wrong — [[feedback_check_the_doc_before_the_index]] in the flesh. **Six of eight rows are DONE (03, 10, 12, 22, 23, 29); what remains is the three `docs/dev/frontend/*` rows.** Wide-read authoring → Fable
- ⏳ [**TASK — write the audit methodology doc**](TASK-write-audit-methodology.md) — PENDING; includes the **CV/segmentor EXCLUSION** and the generalised rule (if correctness is EMPIRICAL not textual, this method produces unfalsifiable noise the verifiers cannot kill). PENDING, fires after the FINAL subsystem audit. Distil the method that works, NOT a diary; raw material captured in the file so the write-up isn't reconstructed from a lossy summary
- ⏳ [**Epoch 2 delta set — v2.0.0..master, classified**](TASK-epoch2-delta-set.md) — INPUT for the doc reconciliation pass, **not the ledger** (nothing written into `docs/dev/deltas/`). Boundary is **v2.0.0 — an epoch is NOT a release (00a §5), so 2.0.1 is a release INSIDE Epoch 2**, not a second baseline. ⚠ **The tracked ledger's Epoch 2 row says "No known code-vs-DR deltas at open" and is 16 commits stale** (18 in the delta set from the anchor), including a shipped release — the exact self-overstatement its own closing paragraph warns about. **Zero of the 18 commits touched a numbered DR doc**; the *testing* doc set WAS maintained, so the discipline routed elsewhere rather than lapsing. §13 makes this a CLASSIFICATION not a list — deferral vs **process failure** ("a delta entry for a change whose documentation was ready is a process failure, not compliance"). Strongest candidate `051ee7bc`: **two rescue mechanisms now exist** — DR's declared per-role `token_sets` and the new global `entity_resolve` — and nothing says which runs first. ~~`MOBILE_TOKEN_EDITOR = true` is hardcoded while call sites still say TEST BUILD = a DECISION, not a doc task.~~ **CLOSED 2026-08-14 — ruled SHIPPED, constant + 9 gates removed (`3667e1f1`), and the user guide it falsified ("Palette and Tokens are desktop-only") corrected; no longer a reconciliation candidate.** `617b8e50` EXCLUDED (foreign, unpushed, blocks `7a0bb1d3`) and should be resolved BEFORE the pass. ⚠ Everything past the commit facts is GREP-LEVEL — read the DR sections whole before filing
- [**SPEC: localized-install act/observe path**](SPEC-localized-install-action-path.md) — issue #51, Chris approved ONE PASS, discovery DONE + adversarially verified, NO code written. **`job_active` misses by ONE WORD** (`SUFFIX_JOB_ACTIVE="_cleaning"` derives key `cleaning`; upstream is `in_cleaning`) → never arms → **every run on a localized install is reaped as `interrupted` ~15 min in, possibly mid-clean**. Plus 3 dead ACT sites (mop-intensity literal, reset buttons, dock buttons). ⚠ HA does NOT raise on a missing service target — it warns — so the safe-water abort is silently dead. Fix = ONE resolver, not a 4th copy of RNF2RCXP.
- [**DOC RESTRUCTURE — state of play**](STATE-documentation-restructure.md) — READ FIRST before touching `docs/dev/`. Three shelves (NOW/DESIGN/HISTORY), DR + epochs retired with reasons, the acceptance test ("does it TRANSFORM the information?" — it inverted twice before landing), the `IN`/`RN` registries, which gate enforces what, and the 7 open items. Counterfactual measurement: authoring + verification DONE, the measurement itself NOT run.
- [Counterfactual pass over 06 — 2026-08-16](FINDINGS-counterfactual-pass-06.md) — built while making the doc acceptance test; **6 of 9 agent 'true answers' were wrong/overstated, all by QUANTIFIER CREEP** (the oracle needed verifying or the doc would be graded against a fiction). 2 LIVE DEFECTS: zone clean **stacks a second dispatch on a running job** (`no_rooms_selected` is not in `_ZONE_CLEAN_INFLIGHT_BLOCK_REASONS`, and the test MagicMocks the reason so it cannot see it — [[feedback_mock_agrees_with_caller]] verbatim); a **multi-room** app-aborted run skips cancel detection (`len(resolved_rooms) != 1`) and is learned as `completed`. NO fixes started.
- [**WAVE 1 — paired Opus/Sonnet doc audit**](WAVE1-report.md) + [findings JSON](WAVE1-findings.json) — 2026-08-16, 463 agents / 36.7M tok / 149 min over 20 units of 12 docs. **379 confirmed findings (185 HIGH), 42 FPs.** THE ROUTING RULE: sweep with Sonnet, walk with Opus, never close a doc on Sonnet alone — precision is IDENTICAL (9.8% vs 10.3% FP, p≈0.88), the whole separation is RECALL. Sonnet holds CURRENT-DRIFT (62% union) + STALE-CITATION (70%) and collapses on FRAME (22%), SOURCE-REPLICATION (1:15), **INTENT-INVERTED (0 of 13)**. Control recall vs the hand-audit: fan-out 5.5/6, Opus 5.5/6, Sonnet 3.5/6. Two limits that a better model will NOT fix: exhaustiveness claims ("the only place X") were checked against the DOC not the CODEBASE by both models, and cross-DOCUMENT contradiction is structurally invisible to per-doc scoping. 477 rationales checked and still honored. 22 judgement calls await Chris.
- [**FINDINGS — the doc walk (OPEN LEDGER)**](FINDINGS-doc-walk.md) — the live ledger for the file-by-file walk of `docs/dev/`. **From `02` onward nothing is fixed in the walk; findings land here and are repaired later from the written spec.** `01` was fixed inline before the ledger existed. Holds 27 doc findings + 6 code findings; **01–05 have been RE-WALKED by change and the drift list is in the ledger (23 commits since v2.1.0-beta.1 changed backend .py with no hand-written dev doc — refine the query to exclude the GENERATED docs/dev/reference/, or it wrongly clears commits like 078ca634).** The re-walk also CLEARED the whole entity-resolution cluster: 21 §6 + 05 §6 are current, so the pattern is specific commits, not "docs are always behind". **THE WALK ORDER WAS WRONG — walk by CHANGE, not by file number; the ledger carries the one-liner. Measured: 20 of the 88 commits since v2.1.0-beta.1 changed backend .py and touched no docs/dev/ — that list IS the backlog.** The file-order pass over 05 walked straight past D-22/D-23, both high-severity. **D-19 is the worst single entry: `06 §6e` still says cancel detection is "single-room jobs only" — the exact gate `078ca634` REMOVED because it was the defect, so the doc now states the defect as the design.** Also **`get_job_inflight_state` is documented NOWHERE in docs/** (D-20). Process pattern, three for three: every stale-gate finding traces to a commit that changed behaviour and touched no dev doc (`8ff4ec15` → 02+04, `078ca634` → 06) — see the release-time review-list suggestion in the ledger. **Carries a STRUCTURAL finding needing a ruling: twice now a doc has routed the reader to 03 as authoritative while being the more current of the two (01 §6 lists 6/7 VacuumBucket keys vs 03's 1; 05 §6 details zone/live_queue/current_phase fields 03 §5b never names). 03 claims the authoritative-shape role without receiving the updates — make it an index, or make a shape change include it.** **D-13 is corpus-level and gates further doc work: EIGHT NOW docs (04/10/12/13/14/16/17/18) declare the RETIRED "re-implement from this document alone" bar in their own Scope line, four more open "Complete implementation reference" — `00 §8` explicitly repudiates that test.** My earlier "blast radius is small, only 23 + README" was WRONG: I greped the retired doc's vocabulary, and the premise survives in docs that never name it. Grep the CLAIM, not the name. Also incl. **02 §8 stale by one day from our own #51 fix** (`b67a90c0` reconciled 06/13/21/22/00b and missed 02), two **Eufy-isms in a brand-neutral doc** (§4 maintenance components, §9 dock triggers are Eufy's verbatim), an exclusivity claim that names the wrong site AND misses a real one (§11 "Vacuum Agent"), and **03's `CapabilityBucket` documented as 6 keys against 41 real** — the missing ones include the entity-binding provenance surface built for the #49/#51 class. **Carries the discriminator that makes the rest of the walk cheap: shapes written as inlined literals are byte-exact, shapes that ACCRETE a key per feature are badly stale — check only the accreting ones.** Read the mode banner before adding to it.
- [**DEFERRED PAST v2.1.0**](POST-2.1.0-deferred.md) — four things found while cutting the release, judged, and deliberately left: the mobile harness shooting its own fixture artifacts (`nullObject` in the frames — fix it in `renderGallery`, NOT the shared gallery fixture, or four pinned baselines churn); the sequence-override row's TWO renderings diverging on a new axis each audit round (colour, queue order, Clear gating — three rounds, three axes); `_render()` having no try/catch, which is how 21 bad calls took the whole card down; and `[SEQ-ORDER-0]` guarding the place the bug WAS rather than where the behaviour lives now. **Feed this to any further audit** — all four have already been re-raised at least once, and the deliberate-do-not-report brief grew every round of five.
- [**OPEN FIX CHECKLIST**](OPEN-FIX-CHECKLIST.md) — every unapplied finding; regenerate with `python .claude/notes/_gen_audit_doc.py` after each audit (manifest: `_audit_runs.json`)
- [Lifecycle hostile audit — 2026-07-30 CALIBRATION](audit-lifecycle-calibration-2026-07-30.md) — active-job lifecycle + exactly-once finalization; 8 agents, 1.9M tok, 41min; **RC-1 = no pre-await finalize claim (5-agent confirm)**, RC-1b cancel race poisons battery aggregates PERMANENTLY, `ended_at` never written (battery post_job dead), dead recharge-end branch, multi-room phase credits room[0] into learned baselines; 1 CRIT + 5 HIGH; 17 over-reaches killed in verification; repair order §9 = ~15 lines for the top 4

## eufy_vacuum — architecture & decisions
- 📉 [**The counterfactual measurement — RESULT**](FINDINGS-cf-measurement-2026-08-22.md) — **RUN 2026-08-22, the measurement STATE §6 had recorded as built-but-never-run.** **`06-job-lifecycle.md` answered 2 of 7 counterfactuals; the source-only control scored 7/7, so AGENT_MISS is ZERO and every failure is the document's.** Shape beats ratio: **zero WRONG** — the doc is either right or silent, never confidently mistaken — and BOTH successes are passages recording a DECISION AND ITS DEFEATED ALTERNATIVE, not narration. Quantifier creep showed up on the READING side too (2 of 7). ⚠ CF-9: the doc FORWARDS to `30-phase-runner.md`, which does not answer it either — a pointer to a place that fails costs a hop and reads like coverage. ⚠ **ONE ORACLE IN EIGHT ROTTED IN SIX DAYS** (`078ca634` fixed the defect CF-6 probed), which is the adversarial review's *"we need both"* thesis demonstrated by accident: identity without a staleness edge decays measurably. Oracles at [ORACLES-counterfactual-06.md](ORACLES-counterfactual-06.md) — **recovered from 2026-08-16 subagent transcripts; they were never written down.**
- 📊 [**Doc campaign — what is left**](DOC-CAMPAIGN-REMAINING.md) — **GENERATED, regenerate after each doc lands** with `python scripts/docs_coverage.py --write`, in the same breath as adding the new doc's row to `scripts/docs_coverage.py::OWNERSHIP`. A hand-kept burn-down drifts in the one direction that flatters. Ownership is DECLARED and the tree audits it, because three inference schemes were tried and all three over-reported — 'the doc that mentions it most' made the invariants REGISTRY the owner of nine subsystems and reported **89% written against eight documents**. ⚠ Package-granular, so `mapping` reads as written while a module inside it may have no coverage; and coverage is not correctness — the claim-verification pass over the first seven docs found 27 real defects in docs this table counted as done.
- 🚧 [**REPAIR BACKLOG — deferred to the end of the doc campaign**](REPAIR-BACKLOG.md) — **the single index of every code defect the doc rewrite found and did not fix.** 3 individual entries (`C54` `C55` `C56`) + 33 battery + 27 rooms + 26 listeners + 29 rooms-map notes + 5 salvage + the 82-item frontend pass. It is a POINTER; the evidence lives in the ledger. ⚠ **Re-verify before editing — an audit finding is a claim, and a false accusation costs more than a miss because someone edits a correct guard on the strength of it.** ⚠ **A single audit pass is a SAMPLE, not an enumeration:** when the listeners workflow was resumed, the two audit agents re-ran on IDENTICAL prompts and returned different sets (22 then 20, sharing 16) — 38% of the union appeared in one run only, including a HIGH that the later run missed entirely. Battery and rooms were single runs; assume both under-enumerate. Over-scoped share varies sharply by subsystem (battery 67%, rooms 55%, listeners 26% — listeners fails by STALENESS instead, both its HIGHs being comments asserting a SHIPPED path is dormant). Highest-value open: **C55** reconciliation pairs any 1-and-1 leftover and calls it a room identity with NO similarity test, then carries settings and access-graph position onto the new id — remedy is NOT obvious, bring it to Chris. **Nothing verifies a line citation inside a source COMMENT** (`check_doc_citations.py` gates docs/ only); C56 found four rotted ones by hand in a single file.
- ⛏️ [**Generator archaeology — what was LOST**](FINDINGS-generator-archaeology-2026-08-23.md) — seven agents read the 4,330-line pre-integration generator by seam, then re-verified every claim against the tree: **34 raw -> 29 verified lost, 12 KILLED as false positives.** Read the kill list first — names changed wholesale in the port, so most apparent losses are present under another name (ordering, CSV exports, dock overhead, charge estimation, retention all survive). ⚠ **Its top finding corrected a document:** `blocked_work_mode` was a REAL gate — arm 4 of an 8-arm pre-flight ladder, testing the exact three values `adapters/eufy/adapter.py` still declares — so `22-adapter-contract.md` §5 and ledger A1/A2 were wrong to call it a check that was never implemented. ⚠ **AND THE CORRECTION WAS ITSELF CORRECTED 2026-08-23:** it was NOT lost in the port either — the 2026-04-02 snapshot carries the ladder in Python, called and enforced. The CALLER was dropped inside the pre-git window, so it entered git already an orphan and `2bfda655` deleted it as one, correctly. Four stages, and only a pre-git snapshot can show the middle two. The vocabulary to restore it is already correct. Also: the map gate INVERTED (an unreadable active map used to refuse a start, now permits one) and the whole estimate used to publish as ~16 HA sensors, chartable and automatable, now reachable only through a service response.
- 🧭 [**Eufy origin lineage**](project_eufy_origin_lineage.md) — ⚠ **READ THIS BEFORE ANY ORIGIN QUESTION.** The pre-integration evolution in ~7 stages and why each forced the next. **Git cannot answer origin questions in this repo — 57 of the project's 172 days predate the first commit.** The earliest artifact is 2026-03-04; the git root is 2026-04-30 and already carries a mature 215-file v0.9.0, so everything in it predates version control; `--diff-filter=A` dates the PATH and `--follow` bottoms out at the IMPORT. Now also carries the **2026-04-02 first-integration snapshot** (`_artifacts/2026-04-02-early-integration/`, 21 files built in one 101-minute session, 28 days before git): the product began as an `after_dependencies` OVERLAY on the community `eufy_clean` integration with no protocol layer of its own — the root of the reverse port — and the slug-demotion, the helper-entity-elimination thesis, and `theme`/`analytics` as empty storage keys are all dated to that night. I burned three exchanges rediscovering history this file already held. Carries: the severed spatial-CV branch and why (no cv2 on the box, runtime coupling, trace capture `sample_count 0`), the 4330-line Notepad-built 'lock-v3 full power' generator that maps ~1:1 onto today's subsystem packages, and the module-size ceiling that began as an agent paste limit and was RETAINED as doctrine after the constraint lifted.
- 🔧 [**Salvaged observations owed to the code**](SALVAGE-observations-for-code.md) — **facts recovered from retired docs that exist NOWHERE in source, owed as comments at the constant they explain.** Grows as the doc rewrite proceeds; nothing is applied during a doc pass so a documentation commit never carries a code change. ⚠ The lifecycle salvage found ZERO (23 candidates, every one already carried by a comment RICHER than the doc) — mapping found three, because mapping touches physical reality and the observations that CALIBRATED its constants were never expressible in code. Sharpest: the Eufy `1 unit ≈ 1 mm` scale is INFERRED, and the tree already carries a confident `1 unit = 1 mm` that is ROBOROCK's — a reader who generalises it treats an unverified approximation as guaranteed. **Keep the hedges; the hedge IS the fact.**
- 📏 [**BN necessity — does this file even need break notation?**](FINDINGS-bn-necessity-2026-08-22.md) — **42 large files assessed 2026-08-22; 41 are ONE IDEA, one needs a boundary.** ⚠ **LENGTH IS NOT EVIDENCE OF STRUCTURE** — using line count as a proxy for *has multiple subjects* is the same error as counting findings instead of scopes. Chris: *"800 lines is not impossible to be a single idea."* ⚠ THE ASYMMETRY INVERTS HERE: a file wrongly left whole costs one extra look; a file wrongly CARVED UP bakes false structure into the anchor registry permanently, so the adversarial pass attacked only the SPLITS and defaulted to overturning — **18 proposed, 17 overturned, 1 survived**. Three recurring overturn arguments (it is a MODE not a sibling / the cut orphans the joiner / one data structure is one idea by construction), the per-file verdict table, and two method failures worth keeping: a hand-transcribed batch list LOST A FILE, and keying verdicts by BASENAME merged four different `manager.py`s. Agrees with [DESIGN-core-manager-gravity-rule.md](DESIGN-core-manager-gravity-rule.md) §3d — a manager that is topologically a post office is one idea by construction.
- 🔍 [**The mixture heuristic — why four buried defects survived the same way**](PROTOCOL-the-mixture-heuristic.md) — **DERIVED 2026-08-21 from four real finds in one day**, not proposed. ⚠ **A defect that only exists in a MIXTURE cannot be found by a test that feeds uniform data.** C17's test fed ONE COMPLETE JOB (a mean over one complete job is correct under both implementations); C40's zone tests always sandwich the zone between room groups, never at index 0; C36 never gets a non-first dt<=0. ⚠ SHARPEST TELL — C15's `test_charge_rates` **works AROUND** the defect by nulling the anchor fields first: **a test that sets up state to AVOID a path is pointing at that path**, and makes the suite green in a way that reads as coverage. Three amplifiers: a zero-guard that covers the DEGENERATE case hides the MIXED one while looking like diligence; the docstring asserted the property the code did not deliver; every line is correct in isolation, so the defect HAS NO SITE and file-walking audits cannot see it. Carries a mechanical scan (find folds with `+=` into an accumulator -> their test call sites -> is any input ever ABSENT while siblings are present). **The first source of attention independent of the ledger** — whose stated MECHANISM was wrong 4 of 4 across tranches 1-2. Doubles as a test-writing rule: feed a mixture in EVERY direction (C17 needed both BM-5b and BM-5c; one numerator served three denominators so the substitution ran both ways).
- 🔴🚿 [**FINDING — the ROBOT's used-water box is riding inside the dirty-water-tank card**](FINDING-robot-water-box-inside-dirty-tank-cards.md) — **MEASURED 2026-09-07, NOT FIXED.** Chris: *"the robot's water tank shouldn't be in our maintenance cards. That has slipped through for a long time."* **35 blocks, every one a `dirty_water_tank` card, also service the ROBOT's used-water box** — two parts on two halves of the machine in one card, the same shape as the clean/dirty merge. 34 are TWO-PHASE (station tank first and contiguous, then the robot box to the end) and 1 is robot-box ONLY (`s60_pro`). ✅ The split is MECHANICAL: the seam is clean and the content is already namespaced `dirty.robot_box_*` — 8 keys, identifiable by prefix with no text matching. ⚠ TWO THINGS MAKE IT A DECISION, NOT A SWEEP: (1) **there is no home** — the corpus has only `clean_water_tank` and `dirty_water_tank`, so it is DROP or a NEW COMPONENT plus a scope.json entry, and the decider is the question that settled the dust box: **does the robot's used-water box have a sensor or counter?** (2) **`s60_pro` would be emptied** — its entire dirty card IS the robot's box, no station tank at all, so stripping leaves nothing (possibly correct: it may never have had a dirty_water_tank in our sense). ⚠ HOW IT SURFACED: Chris caught it in MY OWN WORDS while I was explaining a different defect — I described the 34 as "two-phase cards" as though that were a normal shape, and [[DESIGN-emit-level-coherence-checks]] records x50_ultra's two-phase card as normal to explain why a global step order cannot exist. That is still right about ORDERING; nobody had asked whether the two phases belong in one card at all
- ⏳💧 [**STATE — clean/dirty water tank: the SPLIT (99) and the GAP (137)**](STATE-clean-dirty-water-tank-split.md) — **analysis settled 2026-09-07, NOTHING APPLIED. Read before touching either water-tank component; it exists to stop a re-derivation loop that cost most of a session.** TWO jobs, repeatedly conflated: **THE SPLIT** = the 99 families that HAVE a clean-tank card carrying MERGED dirty-tank behaviour (EMPTY 85% / RINSE 66% / REFILL **8%**, 55 shapes in 99 blocks; `dirty.rinse_plain` sits literally inside 4 clean cards — d20_pro, f10, f10_plus, s10). The vendor's combined section (X20 Max: *"Cleaning the Clean **&** Dirty Water Tanks"*) was lifted into `clean_water_tank`. **The dirty side needs NOTHING** — 167 blocks, EMPTY 100% / RINSE 98% / TOOL-CLEAN 84%, top shape already Chris's spec. **THE GAP** = 137 families with a dock dirty tank and NO clean card at all — a LIFT gap, not a hardware question: *"why would you have a dirty base station tank if you didn't have a clean one?"* A dock that washes pads consumes clean water, which is stored or plumbed, no third source. DO NOT re-open "do they have one". ⛔ **FOUR DEAD ENDS, all walked today:** searching DIRTY blocks for clean content (0 of 168, probe ablated — the merge is in the CLEAN blocks); matching the string "clean water" (158 FALSE POSITIVES — `dirty.rinse_plain` = *rinse the tank with clean water*, the MEDIUM not the part); corpus absence as evidence (CIRCULAR — a family lacks a card because nobody authored one); and "station machine ⇒ has a clean tank" as a creation gate (the Master line is plumbed — [[FINDING-plumbed-variants-have-no-clean-tank]]). Chris's spec: clean = access/remove/**refill**/replace/reverse with cleaning GATED (*"clean if needed"*, backed by `note.float_gentle_clean` ×14 which presupposes cleaning, `note.no_vinegar_in_tank` = scale known but NOT user-treatable, and the existing gated idiom `tank.air_hole` ×42). Split needs NO new keys — `tank.clean_fill` is the vendor's own wording verbatim in l20 p8 / matrix10 p7 / s10_pro_ultra p9. Leave the 17 two-in-ones alone (tank IS the dust box). 5 master/plumbed held out on Chris's call
- ⚠🚰 [**FINDING — the Master line is PLUMBED; a station does NOT imply a clean water tank**](FINDING-plumbed-variants-have-no-clean-tank.md) — **read before creating a `clean_water_tank` block for any family that lacks one.** Chris 2026-09-06: *"ah master variant is plumbed."* Mains water feeds the station through a hookup kit and the station auto-refills the robot's ONBOARD tank — there is nothing for the user to fill. Three manuals: the r2310 group (Master Pro, Master One, X40 Pro, X30 Pro) has **清水箱 = 0 in all five** while carrying 上下水模块; X40 Master 0 with hookup×4 / drain×18; X50 Master has ONE hit and it is a **boilerplate caution about what may be added, not a procedure** — *presence of the phrase is not presence of the part*. ⚠ The corpus count I first offered ("0 master families have a clean-tank card, zero counterexamples") is **CIRCULAR and withdrawn** — Chris: *"dirty only is because we have not split yet[,] cyclical reasoning."* A family lacks a clean-tank card because nobody AUTHORED one; the split is the unfinished work. Only the MANUAL evidence supports this. ⚠ **THE NEAR-MISS:** the clean/dirty split was about to use "dirty card + base station ⇒ give it a clean-water card", which would have shipped a refill procedure for a NON-EXISTENT TANK across the whole Master line. It surfaced only because Chris asked to see the 2 ambiguous families and then their SIBLINGS — master_pro is `dreame.vacuum.r2310` and r2310e/f were already on disk. ⚠ `l20` stops this becoming "plumbed ⇒ no tank": its parts list carries a Clean Water Tank AND a hookup kit, so plumbing can be an OPTION beside the tank. SAFE GATE = the family's own parts list or a real refill procedure, NEVER "it has a station". Consequence: the 74 existing clean-tank cards are safe to restructure; the 142 dirty-only are NOT safe to fill blind — 5 plumbed, 137 need per-family evidence
- ⭐🧱 [**DECISION — the dust box is FOLDED INTO `filter`; do not "fix" an empty filter block**](DECISION-dustbin-folded-into-filter.md) — **SETTLED 2026-09-06 (Chris: *"the filter dustbin is a module"*). Read before touching scope, an empty `filter` block, or any count of them.** The dust box and its filter are ONE MODULE — neither is serviceable alone — so they get ONE card, and `filter` is the half with a purchasable replacement and a counter. Mechanism is one line: `emit_composed.py` sets **`DONOR = {"filter": "dustbin"}`**, so where a family's filter block is empty its **dustbin block is emitted AS the filter card**, inheriting its cadence. ⚠ **THE TRAP:** 81 of 280 families have an EMPTY filter block and a FULL dustbin block, so counting empty filter blocks reads as "81 families ship no filter guide" — they ship the full procedure, and the emitted artifact has **0 of 1,880 blocks with zero steps**. I reported that non-defect to Chris before checking the artifact. ⚠ The fold is NOT cosmetic: of 3,282 placements in the shipped filter cards, **527 (16%) are dust-box CLEANING** — 14 keys across **274 of 280 families** (`bin.empty` 198, `bin.tip_out` 53, `bin.rinse_box_only` 42, intake clean, fill-and-shake) — not merely "remove the dust box". The clincher is `bin.rinse`, **209 users**: *"Rinse the dust box AND filter with clean water"* — one welded sentence, so splitting the component would mean splitting 209 vendor sentences. ⚠ **`scope.json` had drifted three ways and is now fixed:** `dustbin` was in NEITHER `keep` nor `out` (dropped but never recorded as dropped) and `mop_cloth` likewise; and `base_station_filter` / `fluffing_roller` / `mop_compartment` sat in **keep AND out at once**, because all three were cut then RE-ADDED on evidence and the cut rationale was never swept → moved to `_out_superseded_2026_09_05`. Shipping was always right (`emit_composed`, `lint_compose`, `vocab_scan` read `keep` only) but **`build_domain.py:75` iterates `out` to generate docs**, so the generated doc published three SHIPPED components as cut — [[feedback_retirement_isnt_done_until_uncited]] inside a JSON file. OPEN, not decided here: the card is keyed `filter` while 16% of its body is dust-box work — a LABELLING call that lands with [[FINDING-maintenance-heading-not-localized]]
- ⏳ [**Emit-level coherence checks — per step in a block, not per key globally**](DESIGN-emit-level-coherence-checks.md) — DESIGNED 2026-09-06 (Chris's framing: *"this is an emit level check / its not per key its per step in a block"*), NOT BUILT. Rules are block-local: removed→refitted, opened→closed, wetted→dried-before-refit, acted-on→after-its-own-removal, unplugged→plugged-back. ⚠ **THE OBVIOUS IDEA — predetermine the render order — WAS MEASURED AND DOES NOT WORK.** 3,280 co-occurring key pairs are 97.7% order-consistent (99.54% of 45,467 instances follow the dominant direction), which *looks* like a canonical order is almost there, but the dominant relation has **20 CYCLES**, so no total order can exist. The cycles are not incoherence — they are keys playing different roles per block. `access.off_dock` is the clearest: **it is a PHASE BOUNDARY, not a fixed-rank step** — step 1 in a one-phase block, step 6 in `x50_ultra/dirty_water_tank` where 1-5 service the *station's* tank and 6-10 the *robot's* box. I read those six as defective (`rinse_plain` before `off_dock`, 6 vs 6) and **that was wrong**; a global per-key rule cannot express "phase" so it reports legitimate structure as conflict. ⚠ TRACK RECORD IS THE ARGUMENT: block-local checks worked (caster coherence RED 8→GREEN 0; blocks-the-user RED 9→GREEN 0), global ones failed twice (an invented phase taxonomy called a normal vendor order an inversion **95 times**; a regex paired-action linter hit 1401→1050→968 findings against a review that found 68, and was abandoned). ⚠ DIVISION OF LABOUR THAT WORKS: **the corpus PROPOSES** ("217 of 224 blocks put the close immediately before the refit" is how `bin.cover_close` was placed in 66 blocks), **a human RATIFIES**, **the block-local check ENFORCES** — and must go RED on current data first, or it has not been shown to do anything. Do not turn the 97.7% into a gate. Plugs into the new `step_keys`/`note_keys`/`shares_with` provenance in the **debug** artifact (`EMIT_MODE=debug`, 2.24 MB, gitignored; release 1.53 MB unchanged, byte-identical across re-emits) so it runs against WHAT SHIPS — which matters, the artifact was found **stale by 228 blocks**. ⚠ Does NOT replace the canary: provenance is self-reported, so a wrong share-chain resolution reports the wrong source's keys and looks consistent — and that has happened (196 blocks declared a share, 191 held `see`, `shares_with` never fired). `fill()` guards the slot seam; **nothing but a cycle check guards the share seam** — that is the canary's job
- ⏳ [**The rule that keeps core/manager.py from refilling**](DESIGN-core-manager-gravity-rule.md) — **DESIGNED 2026-08-18, NOT BUILT; recovered from transcript 2026-08-21 because it had never been written down.** The head noun is not the discriminator — `themes/`, `dock/`, `onboarding/`, `maintenance/` all carry `manager.py` and stayed 366–833 lines. **The QUALIFIER is: `core` names a POSITION, not a domain, so nothing is "not core" and the name can refuse nothing.** ⚠ RULING: the `orchestrator` rename is ANSWERED NO — the top growers genuinely ARE orchestration, so it ratifies the growth class and becomes a justification. The name that works is **DEALER, NOT PLAYER** (Chris's, 2026-08-18; "an automatic dealer doesn't know the game but can be TOLD how many cards and to who" 2026-08-21 — so the distribution is a DECLARATION, not logic). Operational test, mechanically checkable: **MAY call/route/validate/assemble; MAY NOT compute a domain value or branch on domain state.** `start_selected_rooms` + refusals are legitimate dealing; `get_job_progress_snapshot` deriving phases/estimates is PLAYING. ⚠ Moving the arithmetic out is NOT enough — assembly still couples the manager to the card contract, so the read model shrinks but does not LEAVE until ASSEMBLY moves to a projection module. ("composition root" was my weaker restatement — it says what it may not do; dealer says what it IS.) ⚠ In SEVEN splits the READ MODEL has never once been extracted (get_job_progress_snapshot 506, get_dashboard_snapshot 345) and unlike dispatch its growth STAYED — dispatch is the counter-example, it got moved out three times. Chris's correction: phase_runner 544→2209 is SCOPE EXPANSION, not gravity, and a size gate must tell those apart. Curve: 9486 → 2954 (one day) → **7036**, ~+51 lines/day; an eighth split buys ~78 days, so the RATCHET (a `test_mock_ratchet.py` clone, shrink-only, absent-file-permitted-none) must land FIRST. ⚠ WRITES have their own half — **THE FACTORY** (Chris: *"a write is a target … or a factory could hand off the payload"*): **a write with NO await between its first and last mutation becomes factory-computes/manager-applies; one that awaits mid-span STAYS WHOLE as a TRANSACTION** — one test answers both "refactorable?" and "still correct after?", and decide-then-apply is MORE atomic than the interleaving we have now. 11 of 14 factory-able (re-verified 2026-08-21). `start_selected_rooms` is a transaction AND carries the open `DQ-ACT-6` hazard (a failed start leaves the robot's GLOBAL mop intensity rewritten). Blocks `docs/dev/05-core-manager.md` — but only SURGICALLY: its "things that stay" list justifies itself on WRITE atomicity, which does not reach the three READ aggregators swept in with it; one bullet comes out.
- ⏳ [**DESIGN — generate the facts, hand-write the reasons**](DESIGN-generated-doc-layer.md) — PROPOSED, nothing built, **2.2 scope and RELEASE-INDEPENDENT** (Chris: *"the docs are release independant"*). Split every doc into FACTS (generated from code) and REASONS (hand-written invariants + non-obvious choices). ⚠ The precedent is already in-tree and carries the warning: `THEME_TOKEN_USAGE.md` is generated and once reported **135 tokens with no consumer** — FALSE, the `var()` tracer could not see 3 dynamic families, and acting on it would have deleted live theming. A generated doc cannot drift but is wrong in BULK and carries a NUMBER, which reads as more authoritative than prose. **Two rules fall out: prose cites SYMBOLS never line numbers (adopt this immediately, it costs nothing and retires a whole class), and each generator DECLARES its blind spots with a test guarding them (`DYN_FAMILIES`).** First target `03-services.md` (127 services, 485 rows, has drifted both directions). ⚠ DR consequence, undecided: a generated half is worthless in a real disaster — it regenerates only from the thing you lost, so the PROSE is the only part with DR value and the standard should stop counting generated pages as coverage. Evidence = today's 23 findings, split
- ⏳ [**Bespoke furniture renderer — masks, not SVG**](DESIGN-bespoke-furniture-renderer.md) — PROPOSED, reconstructed from a LOST design session; nothing landed. **Furniture is a material stack with geometry baked into the masks** — theme tokens own appearance, the instance owns only placement, and resolved colours are NEVER persisted per-instance. Reuse the floor COMPOSITOR (`textures/floor-texture-compositor.js`), not the floor problem: no tiling, no per-room offsets, no floor-type resolution. Prototype = ONE BED; if the bed needs bespoke renderer logic the abstraction is wrong. ⚠ **A furniture token family needs a 4th `DYN_FAMILIES` entry in `scripts/gen-theme-token-docs.mjs` or every furniture token will document as DEAD** — runtime-built names are invisible to the `var()` tracer (`7a0bb1d3`). §16 independently reaches the same preview rule as [DESIGN-theme-editor-usability](DESIGN-theme-editor-usability.md): fake the world you hand the renderer, never the renderer. §19 holds 13 genuinely open questions
- ✅ [**Landscape chrome auto-hide**](DESIGN-landscape-chrome-autohide.md) — **BUILT 2026-08-13**; shipped design is now `docs/dev/frontend/responsive-shell.md` (authoritative), this note is the reasoning trail only. Status + nav panes are **89px = 25% of a 360px landscape viewport**, both already `--compact` so that is the floor; hiding both takes the stage 270→359px, and it applies to EVERY view. Scroll-down hides / scroll-up reveals (NOT a taskbar edge-tap — collides with the 44px tap floor). **Pin the status pane while a job is active.** ⚠ The trap: `.evcc-mobile-nav` is `position: static`, so hiding it REFLOWS the stage — must go fixed/sticky + `transform`, never `display`/`height`. The active-job signal ALREADY EXISTS end to end (`jobs/active_job.py:145` → `manager.py:4391` `status in {started,paused}` → `learning.js` `live_queue.active`); it just is not surfaced as a plain accessor, so step 1 is one line, not a new predicate
- ⏳ [**Theme editor — previews, families, disclosure**](DESIGN-theme-editor-usability.md) — **§8 items 1–3 BUILT 2026-08-13** (preview header cut · previews call real renderers, room card done / modal + map still outstanding · chip disclosure); **item 4 family metadata still PROPOSED** and still blocks the Rainbow Bridge hierarchy. ⚠ §7's "token-hint CLIPS" was never true — nowrap alone clipped nothing; struck in the doc. Follows the shipped mobile pass (`5b0fd100`…`115175f8`). **Tokens grouped by MECHANISM, previews drawn by SUBJECT — where they disagree a token is UNPREVIEWABLE** (`--evcc-border-warning`: edge and fill in different groups, the element drawn in a third). Fix = **call the real renderer, never a hand-copy**; `_renderThemePreviewFloorTextureTile` already does. **Map has NO registry entry, `!entry → ""` blanks the column — 34 live tokens edited blind.** ⚠ **"135 with no consumer" in THEME_TOKEN_USAGE.md is a TRAP — ZERO are dead** (3 dynamic families); acting on it deletes live theming. Build order §8; `room.color` parked §9
- ⏳ [**Maintainer Dev Card — manufacture rare stimuli**](DESIGN-maintainer-dev-card.md) — PROPOSED, maintainer-only unshipped card: STALL NOW / fault inject / dev clock. **Principle: manufacture the stimulus, let production own every consequence** (so the #47 renderer must consume the CANONICAL event, not a test path). All trigger seams exist. Clock weighed to **2 actions**: `advance_clock` + `force_tick` (6 safe pollers); pause auto-return is COMPOSED from pause+offset, not a button. **`async_call_later` timers can NEVER be moved by an offset** (5s/180s — not worth a 3rd mechanism). **§8 correction: arming attaches to REACHABILITY and is STATE-DEPENDENT** — advance-clock is inert alone but dispatches `return_to_base` with a job paused, so evaluate at press time
- ⏳ [**Stall capture — cropped map + position (issue #47)**](DESIGN-stall-capture-issue47.md) — SCOPED, not started; **ASSEMBLY not a build** — 5 of 6 components exist on BOTH brands (trigger/raster/bbox/position/history, one coordinate space). **Position is the POSE (`robot_anchor`, pre-normalized), NEVER `robot_position_x/y` = raw uint32 ZUPT-clamped**; RENDER from the room-id raster, do NOT fetch the camera image; port the palette cascade from `map-room-color.js` behind an RF-28-style parity gate (it already drifted once as 2 copies); ±30s pose-ring capture separates wedged from slow
- ⏳ [**Diagnostics tab — questions, routes, health**](DESIGN-diagnostics-tab.md) — PROPOSED, nothing built; out of #46 (a DISABLED entity cost the reporter TEN DAYS) + #49. **The row is a QUESTION, never an entity** — "which map is active" already has 4 shipping answers (2 entities, a declared constant, an inference), so an entity list false-alarms `active_map: MISSING` on an Eufy scalar install that works perfectly. **Three axes, NEVER one enum** — adapter support / route (provenance) / outcome (health); collapse them and `missing` splits into six meanings six months from now. ⚠ **Do NOT reuse `required`** — the schema's one means "the adapter must declare this key" (`entities` is `required: True` while every role inside is optional). ⚠ **Do NOT split `blank` from `unavailable`** — `BLANK_STATE_VALUES` deliberately contains `unavailable` and `is_blank_state` exists precisely to stop callers re-listing that set; carry the RAW STATE STRING alongside instead. First attempt (a disabled check inside `_no_map_message`) was written and REVERTED — backend prose is English-only in an 18-language tab and `escapeHtml` eats the link, so the backend returns the CAUSE and the card writes the sentence. Criticality is a 3-tier on the QUESTION and lives in the schema, not the adapters (15 of 25 roles already state impact in prose; the other 10 are the only real work). Build order §11, dump-before-tab
- [Map-source lifecycle + sticky](reference_map_source_lifecycle.md) — Roborock map HA-mem-only; Eufy .storage-backed; sticky-hold 6h TTL
- [Overview/architecture/deploy](project_eufy_vacuum.md) — state 2026-04-29; docs phase, legacy onboarding removed
- [Locked architecture decisions](project_eufy_architecture_decisions.md) — Option C singleton-domain; room identity TWO-LAYER (room_id + name-slug)
- [Origin lineage](project_eufy_origin_lineage.md) — ~7-stage pre-integration evolution; explains no-helpers/no-CV/timing-first
- [HA quality scale stance](project_eufy_quality_stance.md) — Bronze done; Silver WIP; Gold+ incompatible with adapter pattern
- [Manager split - re-bundled x2](project_manager_split.md) — bundled-subsystem=THE pattern (delegator for prod-callers, repoint white-box)
- [Dispatch-engine seam - phases 1&2](project_dispatch_engine.md) — payload SHAPE + job model adapter-owned; clean_area + pre-calls + live_queue
- [Room seg + job-segmenter seam - v0.9.17](project_room_segmentation_unified.md) — counter-plateau seg + external-run capture + pluggable JobSegmenter
- [Custom segment path - v0.10.0](project_custom_segment_path.md) — many named custom layouts per map + composer; CV stays special
- [Eufy clean_area is global-only](reference_eufy_clean_area_global.md) — rich send_command room_clean = only per-room route; mqtt drops read-back
- [Two-path dispatch concept](project_two_path_dispatch.md) — full-power send_command vs HA-native clean_area (ordered); not built
- [HA native segment/area clean = voice](reference_ha_vacuum_clean_area_segments.md) — CLEAN_AREA + Segment + clean_segments via Assist; -> proxy or PR
- [HA voice WIZARD - multi-turn, no LLM](reference_ha_conversation_agent_multiturn.md) — continue_conversation force-routes follow-ups; hassil CAN'T slot-fill
- [Eufy inter-session coord drift](reference_eufy_intersession_coord_drift.md) — re-localizes origin every session; absolute coords NOT cross-session comparable
- [Boundary derivation DEAD](project_boundary_derivation_dead.md) — trace→room-boundary removed (494c6f6); revive ONLY w/ proven stable-origin
- [Mapping shelve + bounds-off-map](project_mapping_shelve.md) — mapping-inference + RoomBoundsStore DELETED; room tracking 100% off native
- [HA CAN switch Eufy maps - PROVEN](reference_eufy_biz_map_switch.md) — MAP_LOAD over DPS 172; fork PR #150 OPEN; VA switcher PARKED
- [Room ID -> name (vacuum.alfred map_6)](project_room_ids.md) — IDs 1-11 (no ID 10)
- [P2P full-local odds - RESEARCHED](reference_eufy_p2p_full_local_assessment.md) — zero-cloud ~3-10%; p2p_info DORMANT; PIVOT = de-cloud AWS-IoT
- [Roborock adapter - v1.0.0](project_roborock_adapter_planned.md) — per-room clean + native live rollover + per-room fan; S6 mop unsettable
- 📗 [**Roborock dock detection — MEASURED**](REFERENCE-roborock-dock-detection.md) — **read before ANY dock work; supersedes the central handoff §5 where they disagree.** All 27 dock types driven through real HA 2026.8.1 against the VENDOR's own `roborock.testing` simulator → **5 distinct dock-entity signatures**, every boundary a named predicate. ⚠ Two prior claims CORRECTED: the dock device is **not** created unconditionally (it exists iff ≥1 dock entity does), and `child_lock`/`status_indicator` are **not** ungated (the gate is at the call site, not in the description literal — a static read could not see it, the run could). **CALIBRATED against Ivy**: real schema + status reproduce her dock EXACTLY (`model_id='Unknown'`, one entity `mop_drying_status`), and an ablation removing only schema **id 134 `drying_status`** deletes the dock device — so hypothesis 3 is established, not inferred. `roborock.testing` does NOT exist in the 5.5.1 that HA 2026.5.3 pins, which is why this was unmeasurable until now. Holds the 2026.5→2026.7 capability-gating diff (**#46's mechanism**: an upgrade can DELETE dock entities), the enum rename (**integers stable, names renamed — key on the INTEGER**; 5.5.1's old names are PRODUCT names, so Qrevo Curv = 17 and 13 dock names come free without manuals), and the `has_am` caveat (11 of 27 types under-determined from `model_id`). ⚠ **THE NULL-RESULT TRAP** — the first run gave 27 identical empty rows that read exactly like "dock_type gates nothing" and was a silent `SETUP_RETRY`; three guards now stand in the way, keep all three. Harness re-runnable in one command: [`harness/roborock-dock-sweep/`](harness/roborock-dock-sweep/)

## Dreame port (candidate adapter — hardware live 2026-08-09)
- [**Per-room WRITE PATHS — which ones reach the device**](synthesis/dreame-port/WRITE-PATHS.md) — **TWO independent channels, do not treat as one.** `set_cleanset` (all cleaning settings) is BROKEN on stock v2.0.0b25 and **the entity LIES** — it repaints before the transmit call that never runs; locally patched, reported upstream. `update_map_data_async` (floor material) works unpatched and the vendor app confirms it. The bulk `vacuum_set_floor_material` is **absent from services.yaml** and is a **FULL REPLACE**: any segment omitted, or given a non-2-element value, is reset to none — so `{"3":[1,90]}` wipes nine rooms, and YAML int keys wipe *all* of them. Adapters must read-modify-write. Direction is gated on material==WOOD and the entity is ABSENT otherwise (ordering matters) — and those per-room VALUES gate a GLOBAL switch, so a snapshot taken while all rooms are tile reports a live feature as absent. **⚠ EVERY map-data write transmits the WHOLE map — "merge" is not a safety property; a one-room rename rewrote 5 other rooms on live hardware.** Root cause: reader (`set_name`) gives TYPE priority, writer (`segment_type`) gives CUSTOM_NAME priority and forces type 0 — so a leaked `custom_name` sat invisible for weeks and detonated on the first unrelated write. Latent map inconsistency is ARMED, not inert; **validate the whole map before any write**. A Dreame room name is a TYPE CODE, not a user string (app "Master Bedroom" = HA "Primary Bedroom" = type 2, neither is the user's word); read names from the selects or `vacuum.rooms`, never `custom_name`. Carpet is GEOMETRY not a room attribute (3 coexisting lists; "is this room carpeted" is a spatial query) — and declared-vs-detected gives a per-room carpet-recognition confidence read the vendor app cannot show (15% vs 92% on two rooms of one house). Batch tool + restore snapshots in `durable/dreame-port-fixture/`
- [**Manual inventory + i18n language coverage**](synthesis/dreame-port/MANUAL-INVENTORY.md) — device is **L10s Ultra Gen 2** (`dreame.vacuum.r2469a`, SKU RLL32SE). Manuals mirrored to `durable/dreame-port-fixture/manuals/` (10 PDFs, NOT in this public repo — vendor copyright). **15 of the 18 i18n languages are in hand**; cs/ko/zh-Hans genuinely absent with evidenced reasons. **⚠ THE FILENAME IS NOT A MANIFEST** — `R2469B`/`R2469D` carry no language suffix and the site labels them only "EU"/"UK", yet they hold TR+ID and ZH-HK respectively; that under-reporting produced 3 wrong "not available" conclusions in one session (mine + 2 subagents'), all fixed by opening the file. Variant suffix = market, E = Japan; two different files both call themselves R2469B ⚠ Its three negatives (cs/ko/zh-Hans) are **GEN 2 findings, not Dreame-wide** — annotated 2026-08-25 after Korean editions of the X50 (`R2489F`) and X60 Ultra (`R5089F`) turned up in `korean-vocabulary-source/`. Quote them with the scope attached.
- 📘 [**No Dreame simulator — but the capability table is in the source**](REFERENCE-dreame-offline-harness.md) — read before proposing to fake a Dreame. **Verdict: nothing at the Roborock bar.** Upstream tree = 150 paths, ZERO tests/fakes; no PyPI package exists at all (library is VENDORED at `custom_components/dreame_vacuum/dreame/`); `python-miio`'s `MiotSimulator` is real and stateful but the WRONG LAYER — the vendored lib's private siid/piid come back as errors → `None`, and capability detection is `is not None`, so the entity set would be **systematically wrong, not merely incomplete**. ⭐ **The prize instead: `DEVICE_INFO` in `dreame/const.py` is base64+gzip decoding to 390 device entries · 363 capability sets · a 705-entry model-suffix map · ~130 capability booleans, gated `version >= v[1]`** — a per-model capability table readable OFFLINE today, richer than Roborock's (whole devices, not just docks), and vendor data rather than something we author. ⚠ If a fake is ever built we write it OURSELVES against an `is not None` capability model — textbook [[feedback_mock_agrees_with_caller]] with a live detonator; the seam is `DreameVacuumProtocol` (14 members, one import, one construction), traps = `connect()` fires a REAL outbound telemetry POST, and no map manager unless `protocol.cloud` is truthy. §4 is PREDICTED, not run. Does NOT change the Dreame release gate
- 📕 [**Acquisition channels + the three identity maps**](STATE-dreame-acquisition-channels.md) — **read before hunting any Dreame document.** Corpus ~2,900 PDFs / 29 GB across six channels (app API 469 · 16 regional storefronts 242 new · global.dreametech 237 · JP 51 · MOVA US 37). ⭐ **IDENTITY IS DECLARED, NOT INFERRED, THREE WAYS**: the API keys documents to models; **95 reg codes, 38 carrying >1 marketing name** = one certified device (`RLD52SE` proves **L40 Ultra CE = L40s Ultra CE** — the "s" is not a new machine; `RLE15SE` is CROSS-BRAND, Dreame P20 Ultra = MOVA E50 Ultra); and **90 byte-identical clusters over 242 models**. ⚠ The two maps mean DIFFERENT things — reg code = same hardware, shared manual = same DOCUMENTATION, which is the weaker hardware claim but is exactly `A FAMILY IS A MANUAL PAGE`. **242 models collapse to 90 documents**, so authoring is bounded by document count. ⚠ Five costly errors recorded: a **429 storm made every store look absent** (the control failing is the tell); **slug guessing missed Germany's 260 manuals** at `/pages/user_manual` — read `sitemap_pages_1.xml`, never guess; **URLs are PARTIALLY pre-encoded** so only `quote(unquote(path))` round-trips; **a resolved URL is NOT a document** (102 of 585 pointed at dead Xiaomi CDN objects — count FILES); and a **bare-vs-dotted key namespace mismatch faked a 369-model win** that was really zero. **568 of 741 models have a real document; gap 173.** ⚠ `xiaomi.vacuum.*` IS THE MIJIA BRAND (our own manual says `D103CN` / "Mijia All-in-One M30 S") and **the name join fails on it** — Tasshack `Omni M30S` vs manual `M30 S`; join on the KEY. Carries the JA i18n harvest: 45 monolingual docs, every canonical action attested (remove 842 … pull 11) and the full frequency grammar. 4.03 GB of byte-dupes measured, NOT deleted — keep the best-attributed copy
- 🏁 [**Campaign retrospective — "Trust the Bytes" (CLOSED at 717/741)**](RETROSPECTIVE-dreame-manual-campaign.md) — **the manual campaign is CLOSED; this SUPERSEDES the older corpus arithmetic above (the "568/741 · gap 173" in the acquisition line is stale).** ~3,009 PDFs, **717/741 manual-covered = 96.8% = 100% of everything publicly reachable.** Remaining 24 are behind a PHONE-GATED app (22 CN-only MOVA special editions + 2 Mijia M30 `d102`/`d102cn`) — a phone gate, not an effort gap. ⭐ THE ONE RULE: *trust the bytes, distrust every label* — every "absent/dead/non-robot/floor" was wrong ≥1×; re-resolve + SHA-match before believing any absence (we called a floor 5+ times, wrong 5+ times). ⚠ WALLS — do NOT re-run: MOVA `:13267` = AUTH not geo (`Missing token` even from a real CN IP); OSS bucket public-READ but `403` on ListObjects (32-hex hash un-enumerable); app registration phone-gated (VoIP numbers dropped — needs real-SIM); CN web = specs YES / manuals NO (Wenku login-walled). ⭐ RIGHT turns: reg-code Rosetta Stone, MOVA share-QR, **Trouver = the RU brand** (`dns-shop.by` → direct `drv.dns-shop.ru` PDFs), **both-markets rule** (E30 Pro is a US-only "Live Exclusive" — a CN-blank ≠ absent), and the **PARTS-OVER-PAPER pivot** (`derived/maintenance_profiles.md`: 20/20 identifiable walled robots maintenance-sourced from HELD manuals by consumable regime — P60 = Boolean Cloud `RV2401`, brush `VACC-XIA-392` shared across the flagship tier). ⚠ shared part SOURCES steps but never PROVES identity (the confounder). Non-products (not misses): `r9480` Z Series (label), `r2570` N30 Pro (no MOVA product any market). Companion artifact `claude.ai/code/artifact/67344489-1630-4728-b3cd-f6d5bbed5a9a`; derived `coverage_final.json` / `honest_final.json` / `MOVA_WALL_HUNT.md` / `FABLE_GAP_LIST.md`; tools `self_audit.py` / `cn_probe.py` / the workflow `.js`
- 🔧 [**Per-room attribution SCOPE + live-settings findings (2026-08-29 live testing)**](synthesis/dreame-port/SCOPE-per-room-attribution.md) — **"Not reached" on every cleaned room is a CONFLATION**: `job-summary.js:200-224` renders it when a room has no per-room metrics, and Dreame (noop attribution, no area source) never produces them — so a completed multi-room run false-negatives every room. **Fix = the map decoder** (the next build, chosen "proper" over a renderer band-aid): per-room AREA from the ALREADY-decoded map (`camera.robin_map` segments + `'tr'` cleaning trace), per-room TIME from current_room + the EXISTING dwell machinery (`dwell_min_ticks` cap, `_ROOM_EXIT_DWELL_S=20` in active_job.py — current_room tracks rooms but FLAPS at boundaries; dwell debounces it), replacing `noop_room_attribution`; merge `{cleaning_seconds,area_m2}` into `resolved_rooms` before `core/manager.py:3688` enrichment. ⭐ PROVEN this session: **live per-room SUCTION mutation mid-run — a real BEAT-THE-APP** (the vendor app can't; only suction stays writable at launch, route/mode/water lock; `set_cleanset[0]` read-modify-write re-send, ~5 s, enum = our FAN_SPEED_WIRE_MAP; `live_mutable_settings:["fan_speed"]`, buildable on the mid-run blocker loop); **multi-room finalization** confirmed (the `task_type` secondary-clear gate finalized Kitchen→Dining); **route is MOP-ONLY** (coverage Standard 26<Deep 40<Intensive 51 m — "Intensive" tightest, odd naming; inert in vacuum AND vacuum+mop → gate CLEANING PATH on adapter `route_honored_modes:["mop"]`); bulk `vacuum_set_custom_cleaning` MERGES per-segment (NOT the full-replace WRITE-PATHS assumed). Won't-support (Chris's call): mid-run Extra/Exclude area editing — non-room coverage poisons learning
- 📡 [**External-run capture surface — MEASURED near-free (2026-08-30 live)**](FINDINGS-dreame-external-attribution-2026-08-30.md) — **the whole external (app-started, we-didn't-dispatch) lifecycle is already on `camera.robin_map`; nothing to capture, it slots into the existing external-ingest path.** ⭐ **`active_segments` IS the queue AND the tap ORDER — proven by breaking the confound:** run 1 tap 1,3,7 looked ID-sorted, so run 2 tapped **3,1,7** (non-ID) → attribute came back `None→[3,1,7]` (fresh, not stale) AND the robot EXECUTED 3→1→7 (first `cleaned_area`-rise Kitchen→Entryway→Dining). It is a PERSISTED SELECTION SNAPSHOT — holds after the run, does NOT drain, so it gives order but not progress. ⭐ **reached/not-reached rule (order-independent, fixes "Not reached"): a queued room is `reached` when `current_room` is that room AND `cleaned_area` RISES** — `current_room` alone is transit-noisy (dock-in-Dining flip-flop; Living Room crossed twice, never in queue), area-rise filters transit. **READ vs WRITE split:** per-room plan (`suction/water/wetness/mode/route/times/floor/order`) reads off `camera rooms[*]` ANY time incl. mid-run; the matching `select./number.robin_room_N_*` controls go `unavailable` during a run (order gates on `not started`). `rooms[*].order` = full-house DEFAULT order (=room-id, was `[1,3,7]`), NOT the run's tap order `[3,1,7]` — saved config vs run plan. `startup_method` (BY_BUTTON/THROUGH_APP/SCHEDULED/VOICE) is a camera attr on the HISTORY map only (None mid-run), **not a sensor** in this version. ⚠ OPEN: `cleaning_sequence`(`cleanareaorder`) read `null` all run (order came from active_segments, not it); per-RUN settings override unverified (`rooms[*]` is SAVED config); completeness proof (`cleaned_segments`) + live-map `cleaning_sequence` need the in-process probe (restart-gated), not REST. Tapes: `_frozen/dreame-queue-run{1,2}_*_2026-08-30.jsonl`. Complements [SCOPE-per-room-attribution](synthesis/dreame-port/SCOPE-per-room-attribution.md); sibling of the Roborock external-attribution finding
- Sibling docs in [`synthesis/dreame-port/`](synthesis/dreame-port/) — first-run field report, pre-registered predictions, reference reads, builder reports. Not yet hooked individually; read the directory when scoping the port

## Frontend / card / themes
- [Card input focus-restore](reference_card_input_focus.md) — refocuses inputs across re-renders; #37 FIXED v1.6.4
- [Card entity-suggestions - SHIPPED](project_card_entity_suggestions.md) — HA 2026.6 getEntitySuggestion; room+dashboard auto-suggest MANAGED vacuums
- [Card bundle is a build artifact](reference_frontend_build.md) — edit src/, run npm run build:deploy; never hand-edit the minified bundle
- [i18n: never esc() t() output](reference_i18n_double_escape.md) — translate() escapes by default; esc() on t() -> literal &#39
- [Upkeep-guide i18n pipeline](reference_guide_translation_pipeline.md) — guides own py->js sync; empty notes:[] silently shows English
- [Profile-label localization = FRONTEND](reference_profile_label_localization.md) — per-globe via _builtInProfileI18nKey + _localizedProfile; backend English=fallback
- [Theme-token analysis gotchas](reference_theme_token_usage_analysis.md) — scan src CSS + animal-svg + preloaded.py; handle wrapped var(
- [Modal token derivation gotcha](reference_modal_token_derivation.md) — --evcc-modal-* on body-level host, derived from canonical
- [Theme tag/search system - v1.0.0](project_theme_tag_system.md) — auto-facets + colorblind-safe + provenance + vibe-tags; core extracted→theme-kit
- [Render harness + theme gallery](project_render_harness.md) — headless Playwright (visual-reg/CVD/intake) + Pages gallery + theme bot; NOT extracted (niche)
- [Harness UNDER-REPORTS mobile chrome](reference_harness_mobile_measurement.md) — ha-* render 0x0, .evcc-chip exempt from the 44px tap floor; its heights are a FLOOR, measure phone chrome on the device
- [Community animal submission](project_animal_submission.md) — declarative descriptor + sanitise + codegen .js (never accept JS); Wave 1 shipped
- [Mittens - memorial mascot](project_mittens_mascot.md) — baked grey-brown tabby; markings literal/non-themeable, only eye dynamic
- [Run-profile break-insert fix - FIXED 2026-08-02](reference_run_profile_break_insert_fix.md) — CARD-6 clause(1) made Add Charge/Wait a silent no-op + STRANDED new-profile creation entirely (collapsed view's only reveal button); insert-before-last + always-expand + toast

## Testing
- [Don't CI-guard blocking I/O](reference_blocking_io_test_guard.md) — block_async_io unreliable in pytest-homeassistant; rely on runtime detection
- [Testing-doc drift + gap-close](project_testing_drift_gap_pattern.md) — update_test_docs.py owns table cells; digest->audit->apply->gated
- [Eufy adapter test harness - DONE](project_eufy_adapter_harness_planned.md) — brand-agnostic contract suite + Eufy deep tests; segmentor 91%
- [Pre-baked eufy-vacuum-test image](reference_eufy_test_image.md) — deps baked, skips ~78s pip; rebuild on requirements_test.txt change
- [Canonical htmlcov is explicit-only](reference_htmlcov_explicit_only.md) — written ONLY by --cov-report=html:htmlcov; agents use htmlcov_scratch/
- [CI + release gotchas](project_eufy_ci_actions_planned.md) — tests/validate/card-visual; visual change = commit-local→repin→push

## Eufy device/firmware references
- [Factory reset wipes everything](reference_eufy_factory_reset.md) — maps + cloud config gone → new-user; per-map VA data orphaned; check entity_id survival
- [Eufy map editor = geometry not semantics](reference_eufy_map_editor_geometry.md) — refuses wall-less/irregular splits; saved-zone "draw a box" IS the escape
- [Omni E28 (T2352)](project_eufy_e28_support.md) — flagship->'generic'; #38 FIXED v1.6.7; C2 SHIPPED v1.7.0
- [Eufy map outline-offset sign](reference_eufy_map_outline_offset.md) — render offset = (outline_origin-origin)/res (sign INVERTED, fixed 2026-07-02)
- [Alfred error_message always empty](reference_alfred_error_message_firmware.md) — firmware; use binary_sensor.alfred_active_run_has_error; faults ride error[]
- [Alfred clean-completion pattern](reference_alfred_clean_completion_pattern.md) — 5-event signature ~2s after dock; active_run_has_error->off + dock+charging
- [Eufy task_status lifecycle strings](reference_eufy_task_status_lifecycle.md) — mid-run-return vs terminal "Completed"; gates external-run grace
- [Saved-zone run signals SHIPPED](reference_eufy_zone_run_signals.md) — target="N zone"/work_mode=Zone; zone-learning W0-3 done (wall-clock per zone_id+mode)
- [eufy-clean scene select FIRES](reference_eufy_scene_select_fires.md) — select.<obj>_scene FIRES on select_option; arm locally + fire only on explicit
- [datetime-local format varies](reference_datetime_local_parsing.md) — Chrome includes seconds, Firefox doesn't; parse with tolerant regex
- [HA history compact-format rows](reference_ha_history_compact_format.md) — keys s/a/lu/lc not state/attributes; lu/lc float seconds
- [Eufy MQTT push-only staleness](reference_eufy_mqtt_transport_staleness.md) — Alfred update_interval None; push can go silently stale (#160), transport-inherent; reaper backstops
- [**WORKFLOW — screenshots that cannot silently rot**](WORKFLOW-screenshots-and-languages.md) — the pinned language wall (`/hero-shots`, 54 cards, one per language, positions fixed) + the freshness gate (`scripts/check-screenshot-freshness.mjs`) + the re-shoot loop. **Read before re-shooting anything, or before touching a card's strings.** Carries the three ⚠ that break it: the globe is precedence 0 and outranks all 54 pins CROSS-DEVICE (fix is one Auto click, and re-pasting the YAML does NOT fix it — different stores); `--update` BEFORE re-shooting silently disarms the gate forever; adding a locale RENUMBERS every set after its endonym's sort position, so filenames stop matching contents. Also: why the harness does not shoot these (its image has 50 fonts and ZERO for ja/zh/ko/ar/he), and the declared blind spot — `Filter_*.png` guide PROSE is not in en.js, so only chrome is gated.
- [**Semantic Flight Recorder — the protocol**](synthesis/PROTOCOL-semantic-flight-recorder.md) — **READ §5 BEFORE WRITING A SINGLE EMIT SITE.** Carries a RULING (2026-08-25): the call site is UNCONDITIONAL — `recorder.emit(...)` bare, never `if enabled: emit(...)`. The recorder owns the enabled-check, exactly as `receipts.emit()` already does (`isEnabledFor` inside; call sites bare; `receipts/__init__.py` is 100%, 27 stmts, ZERO partial branches). Guarding at the site adds a branch PER SITE, so the project ends up writing tests for its own instrumentation and coverage climbs while nothing got safer. Also records the residual nobody should be surprised by: even the correct shape dilutes the coverage %, which drifts UPWARD as instrumentation grows while risk is unchanged — so stop reading the % as the headline and use the largest-uncovered-BLOCK metric (`scripts/coverage_triage.py`), which is immune. Free to choose now, expensive to unpick from hundreds of call sites later.
- [**Coverage walk — 17 uncovered blocks, read and classified**](FINDINGS-coverage-walk-2026-08-25.md) — **open before writing tests for coverage, or before scoping a test audit.** Not a percentage sweep: every run of >=8 consecutive uncovered STATEMENTS, read in full, classified, REACHABILITY CHECKED. 6 HIGH, all shipped and user-reachable — `set_entity_override` (21 stmts, the largest run in the repo, storage-mutating with NO managed-vacuum check), the strict-order WEDGE-PREVENTION path in phase_runner, roborock dock detection's happy path, reconciliation's second single-pair carry (its SIBLING at :297 is well tested — partial-guard blind spot), the Setup→System census, and stall_capture's C12. Thematic: the entity-override feature shipped in 2.1.0 has ALL THREE surfaces uncovered. Carries two findings that are not coverage at all: mapping_services returns `unsupported_format` for BOTH a corrupt image and Pillow-not-installed, and `core/charging.py` at 100% with zero partial branches misses `battery_level == 0`. Says what it did NOT look at.
- [**FLOW — eufy app guide capture over ADB**](FLOW-eufy-app-adb-capture.md) — **PARKED, validated 2026-09-01; read before re-trying app-capture.** Drive the eufy app UI over `adb input tap/swipe` + read the rendered pixels → bypasses the cert-pinned/auth-gated content API entirely (we never touch it). Proven end-to-end on Alfred (X10). ⚠ **THE STRATEGIC FINDING: app content == the manufacturer's localization coverage, so it is BLIND to our real gaps** — `de` fully translated incl. step text, `he` chrome-only (steps fall back to English), `id` not even offered; our holes are exactly `he`/`id`. It's fully localized only where we ALREADY have rich PDF lift → same source, can't fill a gap the manufacturer didn't. Honest uses: a QUALITY ORACLE (app-`de` vs our lifted-`de`) and native `swivel_wheel` for X10 majors — but that's draft-covered and the app has NO `mopping_cloth` for X10 either. Only shows devices you OWN = one family per device (Alfred=X10). Two live findings baked into the flow: **RTL mirror is NON-uniform** — only the native home mirrors, all RN content screens keep LTR, so the WHOLE path runs on LTR coords (the flip-X trick is unnecessary; home's target is center-tappable); and the **video widget swallows swipes** so scroll the step-band (`swipe 360 1280 360 550`), not the video. Coords calibrated for S23 Ultra 720×1544 — recheck on any other device. Runnable helper `eufy-app-adb-capture.sh`; captured evidence `SAMPLE-eufy-app-capture-proof.json` (en/de/he rolling_brush). App has 29 langs (17 of our 18; missing `id`). Provenance tag = `app`.
- ⛔ [**PARKED — declaring Dreame's consumables, and the proxy blocker under it**](PARKED-dreame-consumable-declaration.md) — **read before declaring ANY dreame maintenance component, or before touching `_consumed_hours`.** Two attempts have been derailed by the same shape: it looks like a four-line declaration and is blocked one layer down. ⭐ **RULING: `caster_wheel` copies EUFY — `proxy_for: "filter"`, 60h, and NOT `maintenance_only`.** It does NOT use upstream's `WHEEL_DIRTY` (siid 30, and ZERO of 143 published MIoT specs have a wheel service, so which wheel it means is unconfirmable; the X50 table lists omnidirectional/caster/main as three cadences). ⛔ **THE BLOCKER, STILL OPEN: a proxied component on a COUNTDOWN brand reads perma-zero.** All three `_consumed_hours` call sites pass the component's OWN meta while the state comes from the PROXY's sensor — `max(60 - 143, 0) = 0` forever. LATENT not live (only eufy proxies, and eufy takes the attribute branch first), and it ARMS the moment a countdown brand declares a proxy — which is exactly what the ruling does, so landing the ruling alone ships the bug. Fix = resolve meta for `proxy_for or component`. ⚠ the suite CANNOT catch it: all 14 maintenance fixtures set `usage_hours`, incl. the "zero usage" case passing `{"usage_hours": 0}` rather than omitting it. Also carries the measured UPSTREAM WALK — 20 consumable properties known, **17 wired to a sensor (the real "can create" set), 16 with resets, we declare 4** — the property→component map, and the three wired to NOTHING (`FILTER_CLEANING`, `TRACK_CLEANING`, **`WASHBOARD_CLEANING`** — so a washboard countdown does not exist upstream for ANY model, while washboard_filter's does). `TANK_FILTER` = siid 17 `waterbox-sieve`, the WATER tank's, spec-confirmed. Consequence for the corpus: **782 of 1,885 composed blocks (41%) reach no card**, and the family gate keys on a DECLARED suffix not a RESOLVED entity, so it cannot express "counter where present, guide card where absent".
- [**DECISION — canonical component-key vocabulary (FROZEN 2026-09-01)**](DECISION-canonical-component-vocab.md) — **read before adding a component key, authoring a family, or mapping a care-corpus component name.** The cross-brand key freeze (Chris's calls): `main_brush` (eufy `rolling_brush` aligns), `caster_wheel` (eufy `swivel_wheel`), `mop_cloth` (eufy `mopping_cloth`), `dust_bag` (roborock `dock_dust_bag`), `dirty_water_tank` (dreame `used_water_tank`). RULE: core owns the canonical KEY, the brand keeps its display WORD — so a shared key must NOT collapse two brands' UI labels. Execution PHASED: dreame `used_water_tank`→`dirty_water_tank` is FREE (unshipped) and rides steps 4–5; the eufy(3)+roborock(1) renames are SHIPPED → ride step 3's content upgrade with dual-accept + a persistence check + per-brand-label reconcile. ⚠ UI labels (`vocab.maintenance_component`) exist for the 7 EUFY keys ONLY; robo/dreame titles fall to a backend label — the label layer is eufy-centric and must be reconciled in the rename. Orphans still open (Battery / High-Speed Maintenance Brush / Hair Collection Box / generic "Water Tank"); `air_duct` is already canonical (dreame has it). Ground truth: `scratchpad/vocab_matrix.py`.
- **Guide CONTENT is GENERATED (eufy/roborock) — content upgrade shipped `3230392d` 2026-09-01.** `eufy_upkeep_guides.py` + `roborock_upkeep_guides.py` + their `upkeep_guides_i18n/*.py` are NO LONGER hand-authored — they carry a GENERATED header and are produced by **`scripts/build_guides.py`** (reshape the lifted TM → per-family/component, canonical keys + orphan mappers) then **`scripts/emit_libs.py --emit`** (provenance-merge vs the pre-swap libs). ⚠ **`emit_libs.py` reads "current" from the BACKUP, never the repo** — because re-emitting against its own output silently corrupts the merge (cost one cycle; that's why it reads the pristine backup + is idempotent). MERGE RULES: **EN** = lift wording for every lift-covered component, current kept where the lift doesn't reach (**0 components lost**); **translations** = a fully-`lift` (real) translation wins → else keep the current real translation (authoritative, never regressed to AI draft) → else a `draft` fills a genuine gap; **frequencies** stay the translated MANUAL phrase ("Einmal pro Woche"), never the lift's English or a machine value. Inputs = the durable TM (`durable/{eufy,roborock}-port-fixture/care_tm_full.json` + `*_care_corpus.json`). **PRE-SWAP SNAPSHOT + recovery: `durable/guide-libs-backup-20260901-072708/`** — the pristine pre-swap libs + shipped content, `MANIFEST.sha256` (93 files), and `SNAPSHOT.txt` (git HEAD `37c23a1f` + porcelain + the 4 TM input hashes). Restore from there if a regen goes wrong. The provenance-merge lives in `emit_libs.py`; the orphan/vocab decisions in [[DECISION-canonical-component-vocab]].
- ⭐ [**SYSTEM — the compose system, its scaling, and the canary**](SYSTEM-composed-guides-and-canary.md) — **read before editing the phrase table, a family record, or any gate over them.** How composition actually works as built, with every figure measured: 589 keys render 14,140 sentence instances = **28.6x leverage**; 33 keys (7%) carry 65% of all output while 34% of keys are used once; the **median new family costs ZERO new phrases**, so cost grows with novel hardware not model count; translation surface 8,415 strings instead of 240,380; 1,791 blocks collapse to 700 distinct shapes, which is what makes a COMPLETE agent review affordable. ⭐ THE CANARY: replace every phrase with a unique marker and render, so placement is checkable without the words — caught the 167 empty filter blocks, sensor.wipe wired as both step and note, the interleaved tank arcs, 3c’s side brush inside main_brush. ⚠ What it CANNOT see: two different keys saying the same thing (x40 flipped the wash tray twice), anything about the words, a MISSING step (a block that never refits looks perfect), and order. Second half: because placement is enumerated, order is a parameter we HOLD, and the render doubles as the authoring spec (which headings each string must read under). ⚠ SIX FAILURE MODES the system produced, all live: the check and the fix asking DIFFERENT questions (561 false positives, would have written a redundant refit into 217 correct blocks); a guard built from the instances in front of you reading as complete (3 pairs swept, 9 existed, 73 defects outside the check); fixing under a running review (288 of 715 shapes changed mid-read); tautological gates; lexical distance measuring LANGUAGE not truth (two wrong rulings reversed); sequence-alignment merging breaking order. Three entry shapes x two container shapes is the recurring trap — three rewrites in one day each handled only the shape in front of them.
- ⭐🔄 [**DESIGN — composed guides REPLACE lift-and-translate**](DESIGN-composed-guide-architecture.md) — **AGREED DIRECTION 2026-09-04; supersedes the TM/translate-tier design. Read before touching `authoring/emit*.py`, `gapfill_*` or the guide libraries.** The corpus stops being the shipped artifact and becomes the EVIDENCE BASE; guides are COMPOSED at emit from a canonical FACT SET. **Why: provenance only ever covered 22% of the matrix** (1,049 of 4,760 cells; median family has ONE vendor locale, 106 have exactly one, 48 none) — so for the other 78% the pipeline spliced fragments from OTHER models' manuals into an English skeleton, and every defect came from the splice, not the text. All of them passed TM fidelity, because byte-exactness cannot see a MISALIGNMENT. Re-gated semantically, failures drop **402 → 98** (→ ~38 if produced this way). ⚠ **THREE ATTRACTIVE WRONG TURNS, recorded so they are not re-derived:** English is NOT canonical (it drops objects freely — `Rinse thoroughly.`; the 94.7% object rate was the writers' choice, not grammar); a locale does NOT render only what its grammar marks (it must render every required dimension, lexically if it has no device); and step-count parity / length ratio across locales are BOTH invalid gates, because a rendering is legitimately shorter where grammar absorbs a dimension. The canonical carries action-sense / object / number / direction / instrument / gate-vs-sequence / referent-binding / exhaustive-vs-illustrative — measured on 15 editions of `e30_ultra`, where **0 of 120 component readings had nothing to add** and each language exposed dimensions the others could not (ar DUAL, zh 方可 permission gate, pl aspect, de/sl case, ar gender fixing the referent). Union for OMISSIONS, flag CONFLICTS. Register = friendly SOP; coverage semi-exhaustive; verification = native readers answering a required-dimension checklist with NO reference to English. Scope trims 25 components → 9 (+2 dock), `dust_bag` to the FAULT surface. ⚠ **The trim does NOT clear the gates** (62% of lint noise, ~10% of real defects). ⚠ **Upstream: the manual extraction reads two-column pages OUT OF ORDER** (E30 Ultra dust-bag comes out 3,4,2,heading,1) and the frequency TABLE is the source of the cadence defect — measure that before authoring from any apparently-thin English guide. Test build (3 contrasting families, 73% phrase reuse by the third) in `authoring/compose/`
- [**Dreame guide families — the SCOPE**](SCOPE-dreame-guide-families.md) — **read before pulling a Dreame manual or authoring a family.** THE RULE: **a family is a MANUAL PAGE**, scope = every `dreame.vacuum.*` key whose marketing name appears on that page. NOT the r-code (X40 Ultra Complete is r2449 but shares X40 Ultra's manual) and NOT the name ("X50" is 23 names; and **one name can have TWO manuals** — X60 Ultra r5089/r9515 vs X60 Pro Ultra Complete r6001 are different robots). **7 families AUTHORED = 75 of 587 keys, 12.8%; every manual in hand is written**, so the next family costs a new PDF. ⚠ AUTHORED and DOWNLOADED are different numbers that this file conflated once — they agree today by work, not definition. ⚠ APPLICABILITY IS ON THE SPECIFICATIONS PAGE, not the cover; four pages also name both variants on the package-contents pages, where "Complete" = spare CONSUMABLES (but on an X60 the same suffix buys a robot — the suffix does NOT generalise across lines). SHARE PROSE ONLY ON A MEASURED DIFF, and read WHERE the delta sits: the two X60 manuals are 48/49 sentences so they share a body, but l50/x50 are 11-of-13 identical and must NOT merge because their delta is INSIDE two components (open-vs-remove cover; LDS-vs-VersaLift). Guarded by tests/adapters/dreame/ — 37 tests, 25 ablations all red. Tooling: `scripts/pdf_layout_dump.py` (visual reading order — no rasteriser exists here) and `scripts/verify_dreame_guide_provenance.py` (264 strings scored, 0 defects; its recast exemption carries a 40% FLOOR or it would hide invented text). ⚠ r501 collides, keys are r+3-or-4 digits with variable suffixes so `r\d{4}` drops ~9%, and X60 Max Ultra Complete is absent from upstream. ⚠ **ALL SEVEN FAMILIES ARE ENGLISH ONLY** and there is NO `upkeep_guides_i18n/` for dreame (eufy/roborock have 17 packs each) — RECORDED NOT STARTED, i18n is LOCKED at 18 langs and Dreame packs need Chris's go. Manufacturer text for most langs is already in the PDFs but uneven (DE/FR 6 of 7 families, AR/ID/JA/TR/ZH-Hant only 1, CS/ZH-Hans 0); the matrix + the two ways the language probe LIED are in the file. ⚠ `ko` — MANUAL-INVENTORY's "absent" is a GEN 2 finding and stands, but Korean IS in hand for x50/x60_ultra (R2489F, R5089F in `korean-vocabulary-source/`); that file is annotated in place.
- `FINDING-maintenance-heading-not-localized.md` - the upkeep modal's component heading renders English for 59% of locale x component; `washboard`/`washboard_filter` have no label key at all. OPEN, deferred by Chris 2026-09-05.
- ⭐🔧 [**DESIGN — key decomposition: a phrase key is not a fact**](DESIGN-key-decomposition.md) — **the level below [[DESIGN-composed-guide-architecture]] §2; read before touching `phrases_en.json` or minting any key.** THE PROBLEM: the keys are PHRASES not words — **487 of 606 (80%) weld two or more canonical dimensions into one sentence**, so no dimension can be reused, corrected or translated alone. The unit becomes a FACT with columns `action/item/item2/limit/dir/instr/purpose`. ⚠ **`purpose` was absent from the design's fact set until 2026-09-05 and the omission was STRUCTURAL** — the dedupe brief's unit is "a phrase is one step", a reason is not a step, so trailing purpose clauses read as commentary and were dropped at the lift (fused ones survived). Our table carries a reason on 2.5% of keys vs 7.1% in the pre-composition library; of 933 cells where SOME locale gives a reason English lacks it in **493**, 98 of them in 3+ locales (`f20/dustbin`: thirteen locales, English silent). 🔴 **This re-explains cut-vs-remove** — not agent invention but a lift loss: `d10s_plus` states the severity condition in its own manual in six languages, and Chris's template IS the source text. ⇒ **A missing dimension is a LIFT-LOSS candidate FIRST, an authoring gap second.** Pilot (`compose/DECOMP-caster_wheel.md`): 21 keys → 29 facts, 8 welding two; it found that `press it back in` is ONE fact across five keys (240 uses) and `do not force` ONE fact welded into seven (457 uses) — both still fully live, both invisible pairwise. ⚠ **The work unit is the COMPONENT, not the key namespace** (the pilot pulled from `access.`/`safety.`/`note.` too; `note.` at 138 keys is not a work unit). Decomposition EXPANDS before it collapses (606 keys -> 765 facts extracted) — that is the point, welded facts cannot be deduped. RULINGS: no fan-out (an agent holding 50 keys cannot see a class spanning five components); DESIGN §8's "71 (action,part) pairs" is SUPERSEDED and must never be measured off the sentences; slots are not next. State: table now 606 keys after the 2026-09-05 collapses; 585 keys still undecomposed; `tool`/`hair_removal_severity`/`clean_water_tank` rulings still PENDING_APPLY.
- 🔴 [**FINDING — number drift, and the parameters trapped in key names**](FINDING-number-drift-and-trapped-parameters.md) — **read before parameterising anything in the phrase table, or trusting a singular/plural pair.** **8% of `main_brush` blocks (23 of 277) mix singular and plural in their STEPS; 227 are consistently singular and 26 consistently plural.** ⚠ I first published this as 32% with ZERO plural — both were my measurement, not the table: `note.brush_injury` is a GENERIC plural caution (Chris), and my pattern matched `brush` inside "the brush GUARD/HOLDER", a modifier not a head noun. **A step must match the count; a caution may use the bare generic plural.** All 23 survivors are ONE shape — lift the main brush out (SG) then pull the brushes out of the holder (PL) — which may be a correct two-level part (assembly → its brushes) rather than a defect; needs a manual. `x50s_pro_ultra` is composed SINGULAR on a machine the X50s Ultra manual shows has "Main Brushes" — it is the only user of the three `_single` keys, which exist solely to serve that one mistake, while its sibling `x50` composes plural. ⚠ **THE RULE: fix the values BEFORE parameterising** — I argued number was a dimension worth lifting out and used these pairs as evidence; the evidence was contaminated. ⭐ **THE SEAM: 95 of 606 keys (16%) encode a parameter in their NAME** (severity 35 · instrument 32 · frequency 17 · number 10 · location 1) because a composition entry is a bare key STRING with nowhere to put a value. That — `(key, dimensions)` — is the real seam, upstream of `{part}` slots and of `template_seam`. The number case is the argument for it: the brief FORBIDS merging on number, so today you can only destroy the fact or duplicate the sentence 50-to-1 and let the copies drift. ⚠ not everything in the 95 is a parameter — `foam.rinse_heavy` / `foam.tap_light` are different ACTIONS. Also carries a lint check that does not exist and would catch all 23 on its first run.
