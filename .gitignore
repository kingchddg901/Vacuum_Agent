# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/

# Test / coverage artifacts
.coverage
.coverage.*
coverage.json
htmlcov/
htmlcov_scratch/
.pytest_cache/

# Local scratch / one-off analysis dir (throwaway; keepers get promoted to tests/ or scripts/)
scratch-external-estimator/

# Node / card build
node_modules/
dist/
*.log
npm-debug.log*

# Render harness build artifacts (baselines under harness/tests/ are committed)
harness/dist/
harness/out/
test-results/
playwright-report/

# MkDocs docs-site build output (built in CI; never committed — docs/ is the source)
/site/
/site-docs/

# Promo/marketing draft stash (forum posts, etc.) — local convenience, NOT part of the published docs site
docs/promo/

# HA user data (never commit)
*.storage
.storage/

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Staging / dev artifacts
mapping_staging/

# Auto-generated at HA startup by __init__.py — not a source file
custom_components/eufy_vacuum/frontend/animal-svg/animals/index.json

# Commit-message scratch files (used by `git commit -F` for long messages)
.commit_msg*
*.commit_msg
COMMIT_MSG*

# Local backup archives
*.zip

# Agent working rules — LOCAL ONLY. Swept into the repo by an unrelated commit
# (c4207b9e, a map-colour feature) on 2026-07-03 and public for five weeks before
# anyone chose to publish it. Still read by Claude Code from disk; just not shipped.
/CLAUDE.md

# Claude Code settings, and the campaign working notes — LOCAL ONLY.
#
# 214 files (10.2 MB) were force-added here during the audit campaign and untracked on
# 2026-08-09: probe/proof scripts, frozen audit JSON, before/after run logs, intermediate
# ledger state. They are working artifacts of a closed campaign, they are still on disk,
# and this repo is PUBLIC — the baseline run logs in particular carried household room
# names. Nothing referenced them: the closeout has zero markdown hyperlinks to its siblings.
#
# No exceptions: the one publicly-linked artifact, the AUDIT-1 closeout, moved to
# docs/audit-1-closeout.md where published things live. Nothing under here is tracked.
.claude/

# Docker cold-install harness — persistent HA config from docker-compose.yml.
# Wipe the folder to test a fresh-from-zero install.
.test-config/

# Scratch output from encoding / corpus analysis. Throwaway by construction — character
# frequency dumps, codec-failure traces, byte-range tallies. Named loosely enough that
# they land in the repo root and read as project files at a glance.
/out*.txt
/scratch_*.txt

# Literal-Windows-path filenames. The default cwd for this project is the PRODUCTION
# config share, so a relative write from a session whose cwd moved creates a file whose
# NAME is the whole intended path. Ignored so the next one is not committed by reflex;
# the fix is always an absolute path, never this pattern.
/C:*

# Per-locale guide JSON emitted by the corpus lift (8.4 MB, ~3.7k files). Intermediate
# between the manual corpus and the generated Python tables that actually ship; nothing
# tracked references it, and this repo is installed through HACS, so shipping the
# intermediates would multiply every user's download for data they never load.
/scripts/guides/
